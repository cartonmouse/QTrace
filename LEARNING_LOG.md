# 重建学习日志

## 目标

通过逐阶段实现一个独立版本，达到能运行、能读代码、能解释设计取舍、能应对面试追问的程度。

## 日志模板

每个阶段完成后补充：

```markdown
### 阶段 N：标题

- 日期：
- 我实现了：
- 入口和调用链：
- 关键数据结构：
- 运行命令：
- 测试结果：
- 我现在能回答：
- 仍然不懂/下一步：
```

## 阶段 1：项目边界与参考代码地图

- 日期：2026-08-20
- 我实现了：冻结参考副本的阅读边界和完整代码地图；确认参考项目包含 React/Vite 前端、FastAPI 后端、五类训练/辅助入口、长期记忆和多种实时/异步协议。
- 运行证据：参考前端 `npm test`、`npm run typecheck`、`npm run build` 通过；`npm run lint` 0 errors；本地合成账号可完成首页 -> 登录 -> ProviderGate。
- 尚未声称：没有真实模型 key，因此没有把 LLM、Embedding、ASR、搜索或实时 Copilot 说成已经验收。
- 下一步：在本目录建立自己的阶段 1 骨架，不复制参考实现。

## 阶段 2：自主实现第一条竖切片

- 日期：2026-08-20
- 我实现了：独立的 FastAPI + React/Vite 工程，包含本地账户、签名 token、密码哈希、provider 配置门禁、SQLite 会话、显式面试阶段状态机、stub provider、复盘和画像写回。
- 后端模块：`backend/security.py`、`backend/store.py`、`backend/provider.py`、`backend/interview.py`、`backend/main.py`。
- 前端模块：`frontend/src/api.ts`、`frontend/src/App.tsx`、`frontend/src/styles.css`。
- 自动化结果：后端 `2 passed`；前端 `npm run typecheck` 通过；`npm run build` 通过。
- 浏览器结果：合成账号完成注册 -> ProviderGate -> 启用 stub -> 开始模拟面试 -> 提交回答 -> 提前结束 -> 复盘 -> 画像；工作台实际运行在本地 `5174`，后端在本地 `8002`。
- 安全验证：两个账户读取对方 session 返回 404；会话查询使用 `session_id + user_id`。
- 我现在能回答：为什么需要 provider 门禁、为什么状态机要显式维护 phase、如何替换 stub provider、复盘如何写回画像。
- 下一步：逐项对照参考项目，增加真正的 provider 接口契约、主题知识库、动态专项出题和历史上下文，而不是直接复制参考代码。

## 阶段 3：Provider 适配层与动态追问

- 日期：2026-08-20
- 我实现了：`InterviewProvider` 协议、会根据阶段内问题编号变化的 StubProvider、OpenAI-compatible Chat Completions 适配器、用户级模型配置接口和首次配置页的真实 LLM 表单。
- 关键链路：`PUT /api/settings` -> `Store.set_openai_provider` -> `engine_for(user_id)` -> `OpenAICompatibleProvider` -> `/chat/completions`。
- 自动化结果：后端 `5 passed`；其中 1 个测试使用 `httpx.MockTransport` 验证兼容接口和 JSON 复盘解析；前端 typecheck/build 通过。
- 安全边界：测试只使用合成 key；设置响应不返回 key 原文；没有调用真实外部模型。
- 当前限制：Embedding 仍是本地 demo 占位；真实模型连通性、超时/重试、流式 SSE 尚未验收。
- 修复的问题：StubProvider 在 technical/project 阶段的第二次追问不再返回相同固定话术。
- 下一步：接入 PDF 简历解析和用户简历上下文，再实现主题知识库与动态专项训练。

## 阶段 4：PDF 简历上传、解析与上下文注入

- 日期：2026-08-20
- 我实现了：用户隔离的 PDF 保存目录、`multipart/form-data` 上传、`pypdf` 文本提取、简历状态/下载/删除接口，以及启动面试时的自动上下文注入。
- 关键链路：`Dashboard` 选择 PDF -> `uploadResume` -> `POST /api/resume/upload` -> `backend.resume.save_resume` -> `pypdf` -> `POST /api/interview/start` 读取文本 -> `InterviewEngine`/Provider。
- 安全边界：只测试合成 PDF；限制 20 MB；拒绝路径型文件名和非 PDF；新文件解析成功后才替换旧文件；没有上传真实简历或调用真实 LLM。
- 自动化结果：后端 `8 passed`；覆盖上传、解析、下载、删除、启动注入和非法文件名；前端 `npm run typecheck`、`npm run build` 通过。
- 我现在能回答：为什么要区分 `FormData` 和 JSON、为什么简历直接作为上下文而不是立刻向量化、为什么需要先解析再替换旧文件、文本输入如何作为 PDF 失败时的降级路径。
- 当前限制：扫描型 PDF 可能提取不到文本；真实系统还需要病毒扫描、加密、配额、保留期限和日志脱敏。
- 下一步：对照参考项目实现主题知识库与专项训练，把“简历上下文”和“知识检索上下文”接到同一个可解释的 prompt 组装层。

## 阶段 5：主题知识库与专项训练

- 日期：2026-08-20
- 我实现了：用户隔离的训练领域、核心知识 Markdown、高频题库 CRUD；领域内容的段落切分和关键词检索；`topic_drill` 启动、出题、追问和复盘；前端专项训练选择页和知识库编辑器。
- 关键链路：`/api/topics` -> `/api/knowledge/{topic}/core` 与 `high_freq` -> `get_topic_bundle` -> `InterviewEngine.start(mode="topic_drill")` -> Provider 使用知识上下文和题库。
- 设计取舍：小语料直接返回全文，大语料使用 top-k 关键词检索；先做可解释的本地检索，保留向量检索替换边界，没有把当前实现夸大成完整生产 RAG。
- 持久化：session 保存 mode、topic、知识上下文和题库快照，保证一轮训练内容稳定、历史记录可解释；旧 Stage 1 数据库通过 sessions 表迁移继续可用。
- 自动化结果：后端 `9 passed`；前端 typecheck/build 通过；测试只使用临时目录和合成知识内容。
- 我现在能回答：为什么要区分简历上下文与领域知识、为什么不是随机题库、关键词检索如何替换成向量检索、为什么要保存训练快照、如何做用户目录和文件名安全校验。
- 当前限制：没有 Embedding、混合检索、重排和知识效果评估；预置领域是面向学习的精简样例，不是参考项目全部领域。
- 下一步：补充专项训练的历史/掌握度视图，并把复盘结果按 topic 写回画像，为后续间隔复习和图谱功能准备数据。

## 项目命名：问迹 / QTrace

- 日期：2026-08-21
- 决定：将独立重建项目正式命名为 `QTrace`，中文名为“问迹”；GitHub 仓库名称暂定为 `qtrace-interview`，目前尚未创建或推送。
- 命名意象：“问”代表面试中的持续提问与追问，“迹”代表把回答、复盘、掌握度和画像变化留下可追踪的成长轨迹。
- 边界：工程目录仍保持 `rebuild`，以兼容当前本地启动命令；界面标题、后端应用名、README、Agent 提示词和前端包名已统一为问迹 QTrace。

## 阶段 6：专项掌握度与长期画像

- 日期：2026-08-20
- 我实现了：`topic_profiles` SQLite 表、按领域累计掌握度、最近得分、薄弱点、专项历史接口和画像页进度条。
- 关键链路：首次 `finish` -> `Store.update_profile_after_review(topic=...)` -> 全局 profile + topic profile 双写 -> `/api/profile` 和画像页展示。
- 幂等性：只有第一次生成 review 才写回，重复打开或重复请求不会重复增加训练次数。
- 自动化结果：后端 `9 passed`；前端 typecheck/build 通过。
- 我现在能回答：为什么要区分全局画像和领域画像、为什么写回必须幂等、平均分为什么只是训练指标、如何进一步替换成难度/时间衰减/间隔复习算法。
- 当前限制：掌握度仍是透明的累计平均分，不等同于真实能力评估；还没有 SM-2 调度、题目难度校准和离线评估集。
- 下一步：实现“今天该练什么”的简单复习调度，之后再考虑参考项目中的更复杂记忆和图谱功能。

## 阶段 6（续）：间隔复习与今日训练队列

- 日期：2026-08-20
- 我实现了：独立的 `review_schedule.py`、`review_items` 调度表、到期复习接口、画像页今日队列，以及专项训练优先复习到期薄弱点。
- 关键链路：`finish` 首次写回 -> `weak_points` 建立或更新 `review_items` -> `/api/profile` 返回 `due_reviews` -> `topic_drill` 将到期项排到题库前面。
- 调度规则：新薄弱点立即到期；成功回忆按 1 天、3 天和 ease factor 延长；低于 6 分则重置到明天。
- 自动化结果：后端 `10 passed`；前端 typecheck/build 通过；测试使用固定日期和临时 SQLite，不修改真实数据。
- 我现在能回答：掌握度和复习排期为什么要拆表、SM-2 的最小状态有哪些、为什么新弱点立即复习、失败为什么重置、为什么调度器应该是纯模块。
- 当前限制：薄弱点使用文本精确匹配，分数还没有经过盲评校准；没有 embedding 去重和知识点图谱。
- 下一步：实现 JD 导入和岗位定向训练，把岗位要求、简历上下文、专项知识和今日队列合成训练计划。

## 阶段 7：JD 导入与岗位定向训练

- 日期：2026-08-20
- 我实现了：`backend/jd.py` 本地 JD 分析器、岗位 preview/start 接口、JD 定向训练页，以及公司、岗位、JD 和分析快照的会话持久化。
- 关键链路：`POST /api/job-prep/preview` -> `analyze_jd` -> `focus_areas / resume_alignment / question_blueprint` -> `POST /api/job-prep/start` -> `InterviewEngine(mode="jd_prep")`。
- 数据组合：JD 分析上下文 + 可选简历文本 + 全局待复习项 + 岗位问题蓝图 -> Provider；对话推进仍复用原有状态机。
- 自动化结果：后端 `11 passed`；前端 typecheck/build 通过；测试使用合成 JD 和临时 SQLite。
- 我现在能回答：为什么先拆 JD 再训练、规则分析器和 LLM 分析器的替换边界、为什么 JD 定向复用状态机、为什么保存 JD 快照、岗位定向和专项训练的上下文差异。
- 当前限制：关键词分析不能理解复杂语义；没有独立的 JD 逐题批量评估页面；真实 LLM preview 尚未接入。
- 下一步：接入真实 LLM 的结构化 JD 分析，并保留规则版作为可测试降级路径；随后实现录音转写复盘。

## 阶段 8：文本优先的录音转写复盘

- 日期：2026-08-20
- 我实现了：`backend/recording.py` 转写解析器和复盘分析器；前端录音复盘页；双人/个人模式；录音会话原文、元信息和复盘结果持久化；结果写回全局画像和复习队列。
- 关键链路：`录音复盘页` -> `POST /api/recording/analyze` -> `parse_transcript` -> `analyze_transcript` -> `sessions` 快照 -> `update_profile_after_review`。
- 设计取舍：当前先实现人工粘贴转写，不接麦克风、ASR 或真实 LLM。双人模式只相信明确的说话人标签，不根据换行臆造问答边界；个人模式把全文视为回答片段。
- 可替换边界：未来使用 `AudioSource -> ASRProvider -> TranscriptDocument -> RecordingAnalyzer -> Review`。ASR 和真实 LLM 都应该是适配器，规则版保留为可测试 fallback。
- 自动化结果：后端 `12 passed`；前端 typecheck/build 通过。
- 我现在能回答：为什么先做文本优先、为什么不能简单按换行切分、录音复盘如何复用统一 Review 和画像、ASR 和复盘分析的接口边界在哪里。
- 当前范围：以转写文本为输入；真实长音频、说话人分离和时间戳对齐不纳入交付目标。评分是可解释的启发式规则。
- 下一步：补充分析器适配层，保留 ASR 作为可选边界，不扩展音频处理复杂度。

## 阶段 9：ASR 与结构化复盘适配层

- 日期：2026-08-21
- 我实现了：`ASRProvider` 协议、`TextPassthroughASRProvider` 本地 mock、`RecordingAnalyzer` 协议、规则分析器和结构化 `LLMRecordingAnalyzer`；录音复盘页增加分析器选择和本地 TXT 导入。
- 关键链路：`analysis_mode=rules` -> `RuleBasedRecordingAnalyzer`；`analysis_mode=llm` -> 用户级 OpenAI-compatible Provider -> `structured_chat` -> JSON 解析/字段归一化 -> 统一 Review。
- 安全边界：LLM 模式必须显式选择且必须配置真实模型；规则版不需要网络；TXT 文件只在浏览器本地读取；测试使用 fake provider 和合成 key，不调用外部服务。
- 设计取舍：路由只负责鉴权和编排，Provider 负责 HTTP，Analyzer 负责业务 JSON；LLM 不能直接把任意自然语言写入画像，后端先校验和限制分数范围。
- 自动化结果：后端 `14 passed`；前端 typecheck/build 通过。
- 我现在能回答：为什么 ASR 和复盘要解耦、为什么需要规则 fallback、为什么 LLM 输出必须结构化校验、为什么真实模型失败时当前不自动降级。
- 范围决策：`TextPassthroughASRProvider` 只用于验证转写来源边界；真实长音频、说话人分离和时间戳对齐明确移出交付范围，真实 LLM 只通过 fake provider 验证契约。
- 下一步：实现 Copilot 的 JSON/SSE 事件协议，再评估是否需要文本版 WebSocket。

## 阶段 10：Copilot 文本 Prep 与 JSON/SSE 事件协议

- 日期：2026-08-21
- 我实现了：`backend/copilot.py` 文本 Prep 逻辑、`copilot_preps` 持久化表、Copilot Prep 查询接口、SSE 事件流和前端 Copilot 页面。
- 关键链路：Copilot 页面提交 JD -> `POST /api/copilot/stream` -> `started` -> `jd_analyzed` -> `risk_assessed` -> `strategy_ready` -> `completed` -> SQLite 保存结果。
- 结果结构：岗位重点、简历匹配、追问策略树、风险地图、面试前行动和问题蓝图；复用已有 JD 分析器、简历文本和全局画像。
- 设计取舍：先实现文本 Prep 和单向 SSE，验证事件协议与持久化；没有把 WebSocket、多 Agent 并行一次性混进来。真实长音频、说话人分离和时间戳继续不属于交付范围。
- 自动化结果：后端 `15 passed`；前端 typecheck/build 通过。
- 我现在能回答：Copilot Prep 与 JD 定向训练的区别、为什么 SSE 适合进度事件、为什么最终结果仍要落库、WebSocket 何时比 SSE 更合适。
- 当前限制：Prep 使用本地确定性分析，没有公司联网搜索、真实 LLM 多 Agent 和 WebSocket 双向实时辅助。
- 下一步：把 Prep 结果与已有训练入口联动，再决定是否实现文本版 WebSocket 实时建议。

## 阶段 11：核心前端闭环整合

- 日期：2026-08-21
- 我实现了：Copilot Prep 历史列表和恢复；Copilot 结果到 JD 定向训练的跨页上下文交接；Dashboard、Copilot、JD 定向、画像之间的下一步入口。
- 关键链路：`GET /api/copilot/prep` -> 恢复公司/岗位/JD 快照 -> `sessionStorage` 交接 -> `/job-prep` 自动填充 -> 重新 preview -> `InterviewEngine(mode="jd_prep")`。
- 设计取舍：Copilot Prep 继续使用独立 `copilot_preps` 表，不混入普通训练 History，因为它不是一场完成的问答会话；跨页只传递明确上下文，进入 JD 页面后立即清理临时数据。
- 自动化结果：后端 `15 passed`；前端 typecheck/build 通过。
- 我现在能回答：为什么使用 sessionStorage、为什么跳转后仍需重新分析 JD、为什么 Copilot Prep 和普通训练 History 分表、如何避免页面之间耦合内部状态。
- 当前边界：核心闭环已连通，但 Personal Agent、知识图谱、完整简历编辑器和 WebSocket 实时辅助仍未完成。
- 下一步：为了匹配 AI Agent 岗位目标，先实现文本 Personal Agent，再做发布前收口。

## 阶段 12：Personal Agent v1

- 日期：2026-08-21
- 我实现了：文本 Personal Agent 页面；用户级 Agent 对话表；画像、SM-2 到期队列、近期训练和简历四个只读工具；规划/回答两步模型边界；Stub 和 OpenAI-compatible 两种模式；对话历史恢复和工具轨迹展示。
- 关键链路：`Agent 页面` -> `POST /api/agent/chat` -> `AgentModel.plan` -> 白名单工具执行 -> `AgentModel.answer` -> `agent_conversations` 持久化。
- Agent 的边界：模型只能提出工具规划，后端负责校验并执行工具；第一版工具没有任何写操作，不会自动修改画像、创建训练或删除数据。
- 个性化上下文：Agent 可以读取长期画像、领域掌握度、SM-2 今日队列、最近训练复盘和上传简历，因此回答不再只依赖当前一句用户问题。
- 真实 LLM 接口：OpenAI-compatible 模式执行两次结构化 Chat Completions，第一次输出规划 JSON，第二次根据工具结果生成回答；本地 Stub 模式不访问网络。
- 自动化结果：后端 `18 passed`；Python compileall、前端 typecheck 和生产构建通过；本地联调成功返回 `plan + tool_trace + answer`，并能恢复对话历史。
- 我现在能回答：普通 LLM 调用和 Agent 的区别、为什么需要工具白名单、为什么 Agent 工具先做只读、如何实现用户数据隔离、规划失败和工具失败如何处理。
- 当前限制：仍是单 Agent 文本版，没有文档向量检索、自动写回长期画像、多 Agent 并行、任务创建工具和 WebSocket 实时辅助。
- 下一步：用本地模型设置接入一次真实 LLM；然后升级专项训练的 LLM 个性化出题和画像结构化抽取。

## 阶段 13：个性化动态出题与画像 v1

- 日期：2026-08-21
- 我实现了：`backend/personalized_drill.py` 出题适配层；Stub 和 OpenAI-compatible LLM 两种出题器；专项训练启动时读取全局画像、领域画像、近期训练、SM-2 到期项和知识上下文；复盘新增强项、行为信号、行动项、最近得分序列和领域趋势写回。
- 关键链路：`POST /api/interview/start(topic_drill)` -> `get_topic_bundle` -> `Store.get_profile/get_topic_profile/list_due_reviews` -> `DrillQuestionGenerator.generate` -> JSON 归一化 -> `InterviewEngine`；结束时 `Review` 写回 `profiles`、`topic_profiles` 和 `review_items`。
- 设计取舍：LLM 只负责生成结构化题目计划，后端负责白名单式字段校验和数量/难度限制；状态机仍由 `InterviewEngine` 统一推进；Stub 仍然优先到期复习项，保证离线可运行和可测试。
- 数据迁移：已有 SQLite 通过 `_migrate_profiles` 增加画像字段和领域得分序列，不删除旧数据库；复盘写回保留幂等性。
- 自动化结果：后端 `23 passed`；Python compileall、前端 typecheck 和生产构建通过；本地黑盒验证确认专项训练可运行、画像新字段可返回、领域趋势初始为 `new`。
- 我现在能回答：个性化出题读取哪些信号、SM-2 如何影响题目优先级、为什么题目生成器和状态机要解耦、LLM JSON 失败如何处理、为什么得分趋势不是完整能力评估。
- 当前限制：还没有使用真实 API Key 做动态出题联调；趋势只是可解释的相邻分数规则；没有知识图谱、Embedding 去重、题目难度校准和 Agent 写工具。
- 下一步：配置用户自己的兼容模型完成真实 LLM 联调，再做发布收口和面试讲解材料；GitHub 创建、提交和推送仍需单独确认。

## 真实 LLM 联调记录

- 日期：2026-08-21
- 用户已明确同意使用当前账号的画像、SM-2 复习项、近期训练和知识上下文进行真实联调。
- Provider 连通性：OpenAI-compatible 请求成功返回合法 JSON，未输出 API Key。
- Personal Agent：`POST /api/agent/chat` 返回 200；完成规划调用、只读工具执行和回答生成，返回非空回答与工具轨迹。
- 动态专项训练：`topic_drill` 启动返回 200；真实 LLM 生成开场和一次追问，追问接口返回 200 且内容非空。
- 结构化复盘：`POST /api/recording/analyze` 的 LLM 模式返回 200；摘要、浮点评分和 4 条行动项成功解析，并生成复盘会话。
- 数据边界：联调使用了当前账号的画像上下文和合成两问两答文本，但没有上传真实简历文件。
- 当前判断：真实 LLM 已经可以被 QTrace 的 Provider、Personal Agent、专项训练和结构化复盘业务链路调用，不再只是底层连通性测试。

## 阶段 14：真实 LLM 稳定性收口

- 日期：2026-08-21
- 我实现了：`OpenAICompatibleProvider` 的超时/网络错误归一化、408/425/429/5xx 有界重试、指数退避和鉴权错误不重试。
- 关键取舍：重试放在 Provider 层，Agent、InterviewEngine 和 Analyzer 不复制基础设施逻辑；结构化 JSON 错误仍然直接校验，不用重试掩盖内容问题。
- 自动化结果：后端 `27 passed`；覆盖 503 重试成功、超时最终错误、网络错误归一化和 401 不重试；Python 内存编译 20 个文件通过；前端 typecheck/build 通过。
- 真实联调证据：阶段 13 的 Provider、Personal Agent、动态出题和结构化复盘均已成功，稳定性边界测试不消耗真实 API。
- 我现在能回答：为什么 Provider 是重试边界、哪些 HTTP 状态可以重试、为什么鉴权错误不能重试、如何避免错误日志泄露 API Key。
- 当前限制：还没有读取供应商的 `Retry-After`、熔断、配额观测和前端细粒度重试按钮。
- 下一步：已进入阶段 15，先实现受控 Agent 写工具，再根据实际使用评估用户确认机制。

## 阶段 15：Personal Agent 受控行动工具

- 日期：2026-08-21
- 我实现了：`create_learning_plan` 工具、`learning_plans` SQLite 表、计划查询接口和前端计划结果卡片。
- 关键链路：用户明确请求计划 -> Agent 规划 -> 读取画像/SM-2/近期训练 -> 受控写入计划 -> 返回计划并保存到当前用户账户。
- 安全边界：非法工具会被白名单过滤；写工具只能在明确的计划请求中触发；工具顺序强制为读取在前、写入在后；不直接修改画像、SM-2 参数、文件或任意 SQL。
- 自动化结果：新增 Agent 学习计划与用户隔离测试；定向 Agent 测试和前端类型检查通过。
- 我现在能回答：为什么先把计划写入独立表、如何防止模型直接改数据库、为什么先用规则生成计划项、如何为后续用户确认和幂等操作预留边界。
- 当前限制：计划项只有 `pending` 状态，还没有完成动作、提醒、确认弹窗和画像自动写回。
- 下一步：进入阶段 16，为 Agent 行动增加用户确认、计划项完成和幂等状态。

## 阶段 16：Agent 行动确认与计划完成

- 日期：2026-08-21
- 我实现了：学习计划 `draft -> active -> completed` 状态、确认接口、计划项完成接口和前端交互。
- 关键链路：Agent 生成草稿 -> 用户点击确认 -> 计划进入执行状态 -> 用户逐项标记完成 -> 全部完成后计划结束。
- 关键取舍：计划项完成只记录行为状态，不直接提高掌握度、不自动修改 SM-2；能力变化仍由专项回答和结构化复盘提供证据。
- 安全边界：确认和完成接口都按 `user_id` 隔离；重复确认和重复完成保持幂等；草稿不能直接完成计划项。
- 自动化结果：覆盖草稿生成、未确认时阻止完成、确认幂等、逐项完成、整体完成和跨用户访问隔离；正式目录后端 `28 passed`，前端 typecheck/build 通过。
- 我现在能回答：为什么 Agent 行动要有确认层、为什么完成计划不等于掌握知识、如何设计状态转换和幂等接口、为什么 status 不能由 LLM 直接决定。
- 当前限制：没有提醒、拖拽排序、计划历史专页和画像/SM-2 自动写回。
- 下一步：用本地账号实际生成并确认几份计划，观察计划质量，再决定是否增加提醒或将完成行为作为画像信号。

## 阶段 17：Agent 学习计划与历史对话绑定

- 日期：2026-08-21
- 真实联调发现：计划生成、确认、完成都成功，但刷新 Agent 页面后历史对话没有重新展示计划操作卡；后端状态已保存，前端没有重新绑定。
- 我实现了：`learning_plans.conversation_id` 字段、旧 SQLite 迁移、创建计划时保存对话 ID，以及打开历史对话时恢复关联计划卡。
- 关键取舍：新数据使用显式对话 ID，旧计划不强行猜测来源；对于旧数据，前端暂时按来源消息做兼容匹配。计划和对话继续分表，分别承担消息轨迹和可执行状态。
- 验证结果：Agent 定向测试 4 项通过；Python compileall、前端 typecheck 和生产构建通过；正式工程的全量回归需在同步后再次执行。
- 我现在能回答：为什么业务状态不能只存在一次性 Agent 响应中、为什么需要显式关联键、旧 SQLite 如何无损迁移、为什么计划和对话要分表。
- 当前限制：还没有独立的计划历史页、提醒、拖拽排序和画像/SM-2 自动写回。
- 正式目录已同步并重启；刷新真实账号页面、重新打开原对话后，计划卡成功恢复，第一项仍保持已完成。
- 下一步：评估计划质量和计划项与专项训练之间的联动，再决定是否增加独立计划历史页。

## 阶段 18：计划驱动专项训练

- 日期：2026-08-21
- 目标：让计划项不只是清单，而是可以进入已有专项训练链路，同时不复制 `InterviewEngine`。
- 我实现了：计划卡的“进入专项训练”入口；`/topic-drill` 支持从 URL 读取 topic/focus；`POST /api/interview/start` 接收可选 focus；Stub/LLM 出题器接收 `requested_focus`；计划摘要显示到期复习的“已安排数/总数”。
- 关键取舍：计划模块只选择训练目标，专项训练页面负责启动训练，出题器负责把焦点转成题目，InterviewEngine 继续拥有唯一的阶段状态机。
- 安全边界：进入训练不会自动标记计划完成；focus 只作为题目生成约束，仍经过题目 JSON 校验、去重、数量和难度限制。
- 到期容量：当前最多安排前 3 个到期项；如果有 6 个，则 UI 明确显示 `3/6`，剩余项继续留在 SM-2 队列。
- 自动化结果：阶段定向测试、正式目录全量测试均通过（`29 passed`）；Python compileall、前端 typecheck/build 通过；运行态健康检查和 OpenAPI focus 字段检查通过。
- 页面验证：真实账号的计划卡出现“进入专项训练”，点击后跳转到专项训练页并显示计划焦点；由于当前焦点属于“综合能力”，页面没有擅自选择具体领域，也没有提交新的真实 LLM 请求。
- 我现在能回答：为什么计划不直接调用 InterviewEngine、focus 如何通过 seam 传给出题器、为什么进入训练不等于计划完成、为什么要透明展示到期容量。
- 当前限制：训练会话暂未保存 plan_id/item_id，完成训练后也不会自动回写计划项；独立计划历史页、提醒和画像/SM-2 自动写回仍未实现。
- 下一步：如果需要更强的可追踪性，再增加训练会话与 `plan_id/item_id` 的审计关联；暂不自动把开始训练或完成训练等同于计划完成。

## 阶段 19：学习计划项与训练会话审计关联

- 日期：2026-08-21
- 目标：让计划项进入专项训练后留下可追踪来源，并在复盘之后提供显式完成动作。
- 我实现了：`StartInterviewRequest` 的 `plan_id/plan_item_id`；`sessions.learning_plan_id/learning_plan_item_id` 字段和旧库迁移；计划项归属/状态校验；复盘页的关联计划卡和“完成这个计划项”按钮。
- 关键链路：计划卡 -> `/topic-drill?focus=...&plan_id=...&plan_item_id=...` -> `POST /api/interview/start` -> 校验 confirmed plan -> `InterviewEngine` -> session 快照 -> 用户看完复盘后显式完成计划项。
- 关键取舍：计划模块只负责目标和来源，InterviewEngine 只负责面试状态；训练开始/结束都不自动完成计划，避免把“产生行为”误判为“已经掌握”。
- 安全边界：关联参数必须成对出现，只能用于专项训练；Store 查询显式带 `user_id`；draft 计划不能进入训练；旧 sessions 只做增量迁移，不删除数据。
- 自动化结果：暂存工程后端 `30 passed`；正式工程后端 `30 passed`；Python compileall、前端 typecheck/build 均通过；运行态健康检查通过，SQLite 已迁移两列。
- 页面验证：真实账号计划入口 URL 已同时携带 `focus`、`plan_id`、`plan_item_id`，专项训练页显示计划焦点；没有提交新的真实 LLM 请求。
- 我现在能回答：为什么关联键放在 session metadata、为什么不让 InterviewEngine 管计划、如何保证用户隔离、为什么计划完成要显式确认、旧数据库如何无损迁移。
- 当前限制：没有计划提醒、独立计划历史页、按成绩自动完成和计划统计；计划完成按钮也不会直接提高画像掌握度或修改 SM-2。
- 下一步：进入发布前收口，先做干净环境复现和 GitHub 版本准备；提交/推送仍需单独确认。

## 阶段 20：发布前行为收口

- 日期：2026-08-21
- 背景：代码审查发现 Agent 写工具的否定意图、计划上下文完整性、复盘加载失败和已完成计划项再次训练存在边界遗漏。
- 我实现了：否定计划请求保护；真实 LLM 计划强制读取近期训练；Review 页面加载失败的可见错误状态；只有完成且有复盘时才显示关联计划完成动作；已完成计划项支持再次训练。
- 关键取舍：不把字符串状态和 plan 参数额外抽象成新模块，避免发布收口阶段引入不必要的结构变化；先修复真实行为风险。
- 自动化结果：暂存工程后端 `32 passed`；Python compileall、前端 typecheck/build 均通过。
- 我现在能回答：为什么 Agent 写工具必须识别否定意图、为什么后端要强制补齐近期上下文、前端如何避免永久 loading、为什么完成项仍然可以再次训练。
- 当前限制：仍未做干净环境复现、GitHub 新提交推送、计划提醒和独立计划历史页。
- 下一步：继续做核心功能完整性评估；等功能完成后再统一进行干净环境复现和公开版本提交。

## 阶段 21：TechSpar 与 QTrace 功能差距审计

- 日期：2026-08-21
- 我完成了：对照冻结参考副本的 `FULL_PROJECT_CODE_MAP.md`、`REFERENCE_RUN.md`、页面入口和能力模块，建立“已复现 / 可解释替代 / 尚未实现”矩阵。
- 关键结论：QTrace 已经具备面试状态机、专项动态出题、画像、简化 SM-2、JD Prep、文本 Copilot、Personal Agent 和计划驱动训练主链，但还没有 Personal Agent 文档库、Embedding/向量检索、题目图谱和结构化简历编辑器。
- 范围决策：实时 WebSocket Copilot、真实长音频、说话人分离和时间戳继续延后，符合此前确认的交付边界；不为了追求页面数量直接复制参考项目的大型简历编辑器子系统。
- 下一阶段：优先设计并实现 Personal Agent 文档库 + `EmbeddingProvider`，先提供本地 deterministic fallback，再接可选 OpenAI-compatible Embedding。
- 验证方式：本阶段未修改运行代码、未调用真实 LLM、未做干净环境复现；现有正式工程基线仍为后端 `32 passed`、前端 typecheck/build 通过。
- 我现在能回答：当前项目与 TechSpar 的真实差距是什么、哪些是有意替代、为什么 Agent 文档库比 WebSocket 更值得先做、如何在面试中诚实描述完成度。

## 阶段 22：Personal Agent 个人文档记忆

- 日期：2026-08-21
- 目标：让 Personal Agent 能读取用户主动保存的项目说明、技术方案和学习笔记，而不是只能依赖画像、训练历史和简历。
- 我实现了：`personal_documents` 和 `document_chunks` 表；文本/Markdown 文档新增接口；按段落/句子边界切分文档；本地确定性 `EmbeddingProvider`；余弦相似度 Top-K 检索；Agent 的 `search_personal_documents` 只读工具；个人 Agent 页面中的文档保存和检索测试区。
- 关键链路：用户提交文档 -> 清理和长度校验 -> 文档分块 -> 本地向量 -> SQLite 持久化 -> Agent 规划只读检索 -> 将标题、分块序号、分数和内容交给回答模型。
- 关键取舍：当前确定性向量使用稳定哈希词元表示，只用于验证 RAG 的分块、存储、相似度和工具链路，不冒充高质量语义模型；以后可替换 `EmbeddingProvider`，不改文档服务和 Agent 工具协议。
- 安全边界：所有文档和文档块查询显式带 `user_id`；Agent 不能写入或删除文档；检索结果限制为最多 8 个片段；本阶段没有把真实资料发送给外部 API。
- 自动化结果：Stage22 定向后端测试 `11 passed`；正式目录独立临时环境全量后端测试 `35 passed`；Python compileall、前端 typecheck/build 和正式运行态健康检查均通过。
- 我现在能回答：为什么整份文档不能直接塞给 LLM、文档块和 Embedding 的关系是什么、本地确定性向量与真正 Embedding 的区别、为什么 Agent 只读个人文档以及如何保证跨用户隔离。
- 当前限制：只支持文本/Markdown 输入，尚未接入 PDF 文档导入、真实外部 Embedding、向量数据库、重排和文档版本管理。
- 下一步：在本地运行态验证文档保存、检索和 Agent 证据链路；随后再评估 PDF 文本复用和可选 OpenAI-compatible Embedding，真实资料外发前单独确认。

## 阶段 23：PDF 文档导入个人记忆

- 日期：2026-08-21
- 我实现了：共享 `backend/document_import.py`；简历上传复用同一套 PDF 校验和 `pypdf` 文本抽取；`POST /api/agent/documents/upload`；个人文档 `source_type="pdf"`；前端 Personal Agent 的 PDF 导入入口。
- 关键链路：浏览器选择 PDF -> FormData -> 文件名/大小/文件头校验 -> 内存字节流提取文本 -> 文件名 stem 生成标题 -> 文档分块 -> 本地确定性 Embedding -> `personal_documents`/`document_chunks` -> Agent 检索。
- 关键取舍：个人文档不保存 PDF 二进制，只保存抽取后的文本和向量；导入阶段不调用 LLM；扫描件没有文本层时明确失败，不引入 OCR；所有查询继续按 `user_id` 隔离。
- 自动化结果：暂存工程完整后端测试 `38 passed`；共享导入器、PDF 上传、无文本层拒绝、旧简历上传和个人文档检索回归测试通过；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么要抽出共享导入模块、为什么 PDF 导入阶段不调用 LLM、扫描件为什么失败、以后换真实 Embedding 需要替换哪一层。
- 当前限制：不支持 OCR、PDF 版本管理、重复导入去重、删除和真实外部 Embedding；个人文档当前仍只保留抽取文本，不保留原始文件下载能力。
- 下一步：评估个人文档检索结果的引用展示、重复导入处理和可选真实 Embedding 适配器，真实资料外发前单独确认。

## 阶段 24：个人文档引用与重复导入收口

- 日期：2026-08-21
- 我实现了：`personal_documents.content_hash` 迁移和按用户索引；规范化正文精确去重；重复导入幂等返回；检索结果 `citation`；Agent 规则回答引用；前端引用展示和重复保存提示。
- 关键链路：文档正文规范化 -> 内容指纹 -> 当前用户下查重 -> 新文档写入或返回旧文档 -> 检索片段补充标题/块序号/短 ID 引用 -> Agent 和前端保留来源。
- 关键取舍：去重只判断规范化正文完全相同，不使用 Embedding 做静默合并；标题和来源类型不参与去重；不同用户始终隔离；旧数据库启动时自动补齐指纹。
- 自动化结果：暂存工程完整后端测试 `40 passed`；覆盖精确去重、旧表迁移、引用字段、Agent 引用和既有 PDF/个人文档链路；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么重复导入要做成幂等、为什么不能直接用 Embedding 去重、引用为什么要包含文档块序号、旧库如何无损补齐指纹。
- 当前限制：只支持精确去重，没有文档编辑、删除、版本管理、引用跳转和语义重复检测；真实 Embedding 仍未接入。
- 下一步：评估文档版本管理和引用跳转，再决定是否接入真实 Embedding；真实资料外发前单独确认。

## 阶段 25：个人文档版本管理与引用版本号

- 日期：2026-08-21
- 我实现了：`personal_document_versions` 版本快照表；个人文档当前版本号；文档更新接口；版本列表和版本详情接口；历史版本载入编辑；检索引用中的 `vN` 版本号。
- 关键链路：编辑器载入历史版本 -> 提交规范化正文 -> 内容指纹判断是否变化 -> SQLite 事务写入新版本快照 -> 更新当前文档和新分块 -> 检索只读取当前分块 -> 返回 `标题#vN-chunk-M` 引用。
- 关键取舍：文档 ID 在编辑中保持稳定，版本号单调递增；标题改变但正文不变时返回 `unchanged=true`，不制造无意义版本；历史版本只读保留，默认不参与检索。
- 迁移策略：旧库启动时为 `personal_documents` 增加 `version`，创建版本表，并把已有当前内容补写成 `v1`；不删除旧字段或用户数据。
- 安全边界：更新、版本列表和版本详情都同时按 `user_id` 和文档 ID 查询；版本列表只返回元数据，用户主动打开某个版本时才返回正文。
- 自动化结果：暂存工程完整后端测试 `41 passed`；覆盖版本更新、版本历史、最新版本检索、无变化更新和旧表迁移；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么文档需要版本快照、为什么不直接覆盖、为什么默认检索只使用最新版本、为什么用内容指纹而不是 Embedding 判断是否变化、如何避免跨用户读取版本。
- 当前限制：没有 diff、回滚、删除/归档和版本引用跳转；个人 PDF 仍只保存抽取文本；真实外部 Embedding 尚未接入。
- 下一步：实现结构化简历编辑器，把简历字段编辑、版本历史、个人文档证据和 Agent 面试上下文连接起来；真实资料联调前继续单独确认。

## 阶段 26：结构化简历编辑器与面试上下文

- 日期：2026-08-21
- 我实现了：结构化简历字段模型（个人信息、概述、技能、项目职责/概述/技术栈/关键工作）；`resume_profiles` 当前表；`resume_profile_versions` 历史表；字段校验和规范化；确定性面试上下文渲染；简历编辑器页面；历史版本载入；Agent/面试/JD/Copilot 的结构化简历回退读取。
- 关键链路：前端字段编辑 -> 后端 Pydantic 校验与规范化 -> 结构化 JSON 指纹 -> 生成 interview context -> SQLite 事务写当前版本和历史快照 -> 面试/Prep/Agent 按优先级消费 PDF 或结构化上下文。
- 关键取舍：不让 LLM 在保存前擅自抽取或改写用户事实；字段事实由用户编辑，渲染器只做确定性格式转换；结构化简历和个人文档分表，因为前者是每个用户唯一的核心身份资料，后者是可检索的多份证据资料。
- 版本规则：首次保存为 `v1`，结构化 JSON 指纹不变时返回 `unchanged=true`，字段事实变化时生成新版本；历史版本只在用户主动载入时进入编辑器。
- 上下文优先级：用户显式提交的 `resume_text` -> 已上传 PDF 文本 -> 结构化简历渲染文本。Agent `read_resume` 结果额外标记 `source=pdf` 或 `source=structured-editor`。
- 自动化结果：暂存工程完整后端测试 `43 passed`；覆盖空编辑器、版本幂等、版本历史、用户隔离、结构化简历注入面试和 Agent 读取；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么简历要字段化、为什么不用 LLM 直接生成 JSON、结构化简历和个人文档如何分工、PDF 和结构化编辑器同时存在时如何选择、Agent 如何读取结构化简历。
- 当前限制：没有教育/工作时间轴、模板排版、拖拽、PDF 导出、diff/回滚和 PDF 到字段的确认式抽取向导。
- 下一步：补充项目字段校验和项目追问卡，把结构化项目证据映射到 Agent 的追问/学习计划；真实资料联调和外部部署仍需单独确认。

## 阶段 27：项目追问卡与个人证据映射

- 日期：2026-08-21
- 我实现了：根据每个结构化项目生成背景与目标、个人职责、关键设计、效果验证、复盘取舍五类追问卡；卡片保存 `field_refs` 和解释性 `document_query`；通过个人文档本地检索返回最多 3 个带 citation 的证据片段；前端展示追问卡和证据引用。
- 关键链路：当前结构化简历项目 -> 确定性问题模板 -> 结构化字段引用 -> 项目名+问题类别检索个人文档 -> 返回当前版本 citation -> 用户按卡片准备回答。
- 关键取舍：先用确定性模板保证问题覆盖和可复现，再考虑让 LLM 在固定类别内润色；没有个人文档证据时仍返回问题，把缺少材料暴露为准备缺口，不编造事实。
- 安全边界：追问卡接口只读结构化简历和个人文档，不修改简历、画像、SM-2 或学习计划；个人文档检索继续按 `user_id` 隔离。
- 自动化结果：暂存工程完整后端测试 `43 passed`；覆盖五类卡片、项目证据命中、简历版本号同步和既有个人文档链路；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么先用确定性问题模板、为什么项目名可以作为简历和个人文档的 seam、没有证据时为什么不报错、追问卡为什么不能自动修改画像或学习计划。
- 当前限制：问题模板暂不感知具体 JD 和掌握度；证据查询没有 reranker 或 query rewrite；卡片还不能直接跳入专项训练。
- 下一步：把追问卡接入 JD focus、SM-2 到期复习和专项训练入口；真实资料联调和外部部署仍需单独确认。

## 阶段 29：Agent 追问卡计划

- 日期：2026-08-21
- 我实现了：`AgentChatRequest.question_card_id`；`read_question_card` 只读工具；当前用户/当前简历版本校验；项目追问卡驱动的 draft 学习计划；计划 `source.question_card` 和计划项中的卡片元数据；结构化简历页面到 Agent 的入口；计划项继续把卡片 ID 传入专项训练。
- 关键链路：追问卡 -> `/agent?question_card_id=...` -> `POST /api/agent/chat` -> 强制读取卡片 -> 明确计划意图才调用 `create_learning_plan` -> draft 计划 -> 用户确认 -> 计划项进入专项训练 -> session 保存来源。
- 关键取舍：不从自然语言猜项目名，不让模型直接决定卡片事实；客户端只传卡片 ID，后端按当前用户重新验证。卡片计划项先使用“综合能力”，进入专项训练时由用户选择具体领域，避免把跨领域项目强行映射到错误题库。
- 安全边界：无效或跨用户卡片在创建 Agent 会话前直接返回 404；卡片读取是只读工具；计划仍然是 draft/active 状态机；点击计划项或训练结束不会自动完成计划。
- 自动化结果：暂存工程后端 `44 passed`；覆盖卡片来源、draft 计划、计划项元数据、用户隔离和非法卡片访问；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么卡片 ID 必须由后端验证、为什么需要强制 `read_question_card`、为什么计划仍然要确认、为什么计划项不能直接绑定一个默认领域、为什么这不是多 Agent 而是受控工具扩展。
- 当前限制：卡片仍需用户选择训练领域；Agent 不会根据 JD 自动调整项目卡的领域权重；draft 计划暂未提供独立取消/归档操作。
- 下一步：继续完善 JD focus 与项目字段的匹配解释，再统一进行干净环境复现；真实资料联调、外部部署和 GitHub 推送仍需单独确认。

## 阶段 30：JD 与项目追问映射

- 日期：2026-08-21
- 我实现了：`SKILL_CATALOG` 到结构化项目字段的确定性匹配；`project_matches` 返回项目名、岗位 focus、命中技能、命中字段、解释和代表性 `question_card_id`；JD 定向备面页面的“JD → 项目追问映射”区域；从命中项目直接进入 Agent 计划入口。
- 关键链路：JD 技能识别 -> 当前用户结构化项目 -> `description/role/technologies/highlights` 字段命中 -> 关键设计或效果验证追问卡 -> Personal Agent 验证卡片 -> draft 计划 -> 专项训练。
- 关键取舍：第一版不让 LLM 猜项目相关性，也不把 PDF 长文本强行拆成项目；结构化简历不存在或用户关闭简历联动时返回空项目映射，但仍保留普通 JD 技能分析；命中“评测/性能优化”时选效果验证卡，其他技能选关键设计卡。
- 安全边界：匹配结果是准备建议，不是录用判断；只读结构化简历，不写画像、SM-2 或简历版本；返回的卡片 ID 进入 Agent 后仍要按用户和当前简历版本重新验证。
- 自动化结果：暂存工程后端 `45 passed`；新增 JD 项目映射、简历开关行为和追问卡 ID 测试；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么 JD 匹配要使用结构化简历、为什么不让 LLM 直接判断匹配、为什么关键词命中不能代表岗位适配、为什么评测技能映射到效果验证卡、为什么这一步不需要向量数据库。
- 当前限制：技能词表覆盖有限，没有语义匹配/重排/置信度，一个项目暂时只返回一张代表性卡片。
- 下一步：继续补充岗位 focus 权重和用户可确认的匹配解释，再统一进行干净环境复现；真实资料联调、外部部署和 GitHub 推送仍需单独确认。

## 阶段 31：JD focus 权重与项目匹配评分

- 日期：2026-08-21
- 我实现了：项目映射中的 `priority` 和 `score`；JD 技能出现次数、`focus_areas` 优先级和项目字段权重的确定性评分；按分数排序项目；JD 页面展示 high/medium/normal 和匹配分数。
- 关键链路：JD 技能频次 -> focus 优先级 -> 项目字段命中 -> `technologies/role/highlights/description` 权重 -> 项目总分 -> 代表性追问卡 -> Agent 计划入口。
- 关键取舍：技术栈权重最高，但职责和关键工作也必须参与；分数只决定准备顺序，不代表录用概率或能力评分；同分按项目名稳定排序，保证回归测试和面试解释一致。
- 安全边界：评分只读 JD 和结构化简历，不修改画像、SM-2、简历版本或 Agent 计划；Agent 计划仍需要显式卡片验证和用户确认。
- 自动化结果：暂存工程后端 `45 passed`；新增评分断言；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么技术栈权重最高、为什么 high 不是录用概率、两个项目分数相同如何处理、为什么匹配结果不能直接修改 Agent 计划。
- 当前限制：技能词表仍是人工维护，优先级只看出现顺序，没有语义重排、置信区间或用户反馈校准。
- 下一步：增加用户确认的项目首选项和更细的岗位 focus 解析，再统一进行干净环境复现；真实资料联调、外部部署和 GitHub 推送仍需单独确认。

## 阶段 32：知识图谱最小读模型

- 日期：2026-08-21
- 我实现了：主题知识图谱后端读模型、`GET /api/graph/{topic}` 接口、前端“知识图谱”页面，以及高频问题、相近问题和 SM-2 到期复习点的关系展示。
- 关键链路：主题知识库/高频题库 + `topic_profiles` + `review_items` -> `build_topic_graph` -> Pydantic 图谱响应 -> SVG 关系图和关系明细 -> 一键进入专项训练。
- 关键取舍：参考项目的图谱使用评分问题和向量相似度；QTrace 当前采用用户隔离、可重复构建的确定性 token 重叠规则，并对中文补充二元字符片段。图谱是 read model，不新增 Neo4j，也不复制一份容易与题库分叉的图数据。
- 节点类型：主题根节点、高频问题节点、SM-2 待复习节点；问题节点状态可以是待练习、薄弱关联或今日复习。关系类型包括主题包含问题、问题相近、主题待复习和问题回访复习点。
- 安全边界：所有主题文件和复习数据都按当前用户读取；未知主题返回 400；图谱只读，不会因为打开页面改变画像、复习间隔或训练记录。
- 自动化结果：暂存工程后端 `47 passed`；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么图谱不应成为第二份事实源、为什么 SM-2 节点比单纯画题目更有价值、中文 token 重叠规则有哪些局限、为什么图谱页面不需要 LLM、为什么图谱读取必须继续做用户隔离。
- 当前限制：关系边是可解释的词法近似，不等同于 Embedding 语义相似；当前图谱按主题查看，尚未根据图结构直接生成下一题或自动调整复习参数。

## 阶段 33：图谱到 Agent 计划联动

- 日期：2026-08-21
- 我实现了：`AgentChatRequest.topic` 主题作用域、图谱页的“让 Agent 安排复习”入口、主题校验、主题过滤的 `read_due_reviews` 和计划来源记录。
- 关键链路：`/graph?topic=rag` -> `/agent?topic=rag` -> `POST /api/agent/chat(topic=rag)` -> 后端确认用户拥有该主题 -> Agent 只读取 `rag` 的 SM-2 到期队列 -> draft 计划 `source.topic=rag` -> 用户确认 -> 现有计划项专项训练和 session 审计。
- 关键取舍：不新建图谱专用计划表，也不把图谱页面访问当成学习行为；主题只是 Agent 工具读取范围和计划来源元数据，实际画像/SM-2 更新仍然只发生在完成训练并复盘之后。
- 安全边界：主题由后端按当前用户的 `topics.json` 重新校验；未知主题返回 400；Agent 仍只能使用受控读工具和确认后的计划写工具，不能根据前端传来的字符串读取其他用户数据。
- 自动化结果：暂存工程后端 `48 passed`；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么 topic 作用域要在后端校验、为什么只过滤 SM-2 读取还不够必须记录计划来源、为什么图谱到 Agent 仍然要保留 draft/confirm、为什么不能在打开图谱时直接更新复习状态。
- 当前限制：计划项仍然是主题级复习，尚未精确绑定到图谱中的某一条问题节点；下一步可以增加问题节点到专项训练的确定性选择规则。

## 阶段 34：图谱问题到精确训练

- 日期：2026-08-21
- 我实现了：`graph_question_id` 请求字段、当前用户题库中的问题解析、图谱问题到专项训练的按钮/节点入口、训练会话中的 `graph_question_id` 与完整问题文本审计字段。
- 关键链路：图谱 `question:<n>` -> `/topic-drill?topic=rag&graph_question_id=question:2` -> 后端重新读取当前用户 RAG 题库并校验 ID -> 个性化出题器生成候选 -> 把指定问题稳定放到题库首位 -> InterviewEngine 首个技术追问使用该题 -> session/复盘展示来源。
- 关键取舍：不让前端直接提交完整问题并信任它，也不绕过个性化出题器另起状态机；只接受可验证的节点 ID，完整问题由后端事实源解析，之后仍保留画像、SM-2 和动态候选问题。
- 安全边界：图谱问题必须属于当前用户、当前主题和当前题库；非法 ID、跨主题 ID 或非专项训练模式都会被拒绝；打开节点只导航，不写画像或复习队列。
- 自动化结果：暂存工程后端 `49 passed`；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么要保存问题文本而不仅是 ID、为什么不能让前端传完整问题、为什么指定问题仍然要经过动态出题器、为什么后续追问不必全部锁死、为什么来源字段要进入 session 而不是只留在 URL。
- 当前限制：问题节点目前作为首个技术追问，后续问题仍按主题题库和画像动态生成；尚未把具体问题 ID 继续传入 Agent 计划项。

## 阶段 35：图谱问题到 Agent 计划

- 日期：2026-08-21
- 我实现了：`read_graph_question` 只读 Agent 工具、`AgentChatRequest.graph_question_id`、计划来源中的图谱问题、计划项中的 `graph_question_id/graph_question` 元数据，以及图谱问题到 Agent 计划的前端入口。
- 关键链路：图谱问题节点 -> `/agent?topic=rag&graph_question_id=question:1` -> Agent 强制读取当前用户当前主题题库 -> `create_learning_plan` 生成 `graph_question` 计划项 -> 用户确认 -> 计划项回到 `/topic-drill` 并携带图谱问题 ID -> Stage34 的 session 审计。
- 关键取舍：Agent 不直接信任前端传来的完整问题，也不把图谱复制成第二套事实表；它通过只读工具按用户和主题重新解析节点，再把解析结果作为计划来源快照保存。
- 安全边界：缺少主题、未知主题、未知问题 ID 都会被拒绝；`read_graph_question` 是只读工具；只有明确计划请求才允许 `create_learning_plan`，计划仍需 draft/confirm；计划项进入训练时继续复用后端问题校验。
- 自动化结果：暂存工程后端 `50 passed`；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么需要 `read_graph_question` 工具、为什么计划项需要同时保存 ID 和文本、为什么不能只在前端拼 URL、为什么 Agent 计划仍然不是多 Agent、为什么这条链路没有新增图数据库。
- 当前限制：真实 LLM 的规划器还只是看到工具契约，后端会强制补读图谱问题；图谱关系质量仍是确定性词法近似，尚未用训练结果校准边权。

## 阶段 36：图谱相近问题候选

- 日期：2026-08-21
- 我实现了：图谱问题节点的 `related_question_ids` 派生字段；Agent `read_graph_question` 返回最多 3 个相近问题候选；Agent 计划 source/首个计划项保存可选后续练习；前端图谱页支持点击节点查看相近题并分别进入训练或 Agent。
- 关键链路：问题之间已有 `related` 边 -> 图谱响应派生邻居 ID -> 用户点击节点 -> 查看相近问题候选 -> 可选进入 `/topic-drill` 或 `/agent`；从 Agent 生成计划时只把候选作为元数据，不自动创建额外计划项。
- 关键取舍：不重新计算第二套相似度，也不把相近题自动塞进计划；候选沿用图谱边的确定性规则和权重，用户明确点击后才产生下一步动作。
- 安全边界：邻居来自当前用户当前主题的题库；Agent 读取工具会重新构建当前图谱；候选只是只读建议，不修改画像、SM-2 或计划状态。
- 自动化结果：暂存工程后端 `50 passed`；Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么 related 邻居应该从已有边派生、为什么不自动扩张计划、为什么相似题只能作为候选、为什么候选仍然需要用户点击、为什么图谱边权不能直接当成能力置信度。
- 当前限制：相近关系仍是词法近似，候选排序还没有使用真实 Embedding 或训练结果反馈校准。

## 阶段 37：图谱候选训练反馈审计

- 日期：2026-08-21
- 我实现了：`graph_entry_source`、`graph_parent_question_id` 和父问题快照；后端对 related candidate 做重新建图校验；图谱 related 边按当前用户/主题统计训练开始和完成次数；Agent 计划与专项训练继续传递候选来源；图谱页和复盘页展示来源与反馈次数。
- 关键链路：相近题候选 -> `graph_entry_source=related_neighbor` + 父节点 ID -> 后端验证父子关系 -> session 保存来源快照 -> 训练/复盘更新完成状态 -> 图谱 read model 回显 started/completed 统计。
- 关键取舍：统计行为而不是修改关系；`related.weight` 仍是确定性词法关系强度，SM-2 和画像仍只由复盘评分更新；普通图谱节点训练不会混入候选反馈。
- 安全边界：候选父节点只能属于当前用户当前主题，且必须存在于当前图谱的 `related_question_ids`；不接受前端直接提交的父问题文本；旧 session 默认兼容为空，旧图谱 URL 按 `question_node` 处理。
- 自动化结果：暂存工程后端 `51 passed`；Python compileall、前端 typecheck 和生产构建通过；覆盖来源保存、非法父节点、开始/完成统计和 Agent 计划回流。
- 我现在能回答：为什么要保存父节点快照、为什么统计完成次数而不是直接改边权、为什么普通图谱训练不能算候选反馈、为什么 Agent 计划也必须携带来源、为什么行为反馈还不能证明学习效率提升。
- 当前限制：统计样本还很少，只能说明用户是否使用候选入口，不能证明候选的语义质量或学习收益；真实 Embedding 和离线评估尚未接入。

## 阶段 38：图谱候选离线评估

- 日期：2026-08-21
- 我实现了：`GET /api/graph/{topic}/feedback` 只读报告；按当前 related 边聚合候选训练的完成率、有效复盘平均分、首末分差和重复训练比例；图谱页增加候选反馈评估面板。
- 关键链路：当前用户/主题的 `related_neighbor` sessions + `review_json` -> 按父节点/目标问题边聚合 -> 过滤当前仍存在的 related 边 -> 返回评估报告 -> 前端展示“候选关系/有行为关系/开始/完成”和逐边指标。
- 关键取舍：没有有效复盘分数时返回空而不是 0；开始次数仍包含未完成 session；分数变化只做描述性统计，不自动改 related 权重、SM-2 或画像。
- 安全边界：报告严格按 `user_id`、主题和候选来源过滤；普通图谱节点训练、Agent 主题计划和其他主题不会混入；题库关系变化后报告只保留当前图谱边。
- 自动化结果：暂存工程后端 `51 passed`；Python compileall、前端 typecheck 和生产构建通过；覆盖无样本报告、开始/完成聚合、有效分数和重复训练比例。
- 我现在能回答：为什么评估接口必须是只读、为什么完成率不能代表能力提升、为什么 `score_delta` 需要至少两次有效复盘、为什么失败复盘不能伪造成 0 分、为什么要过滤当前不存在的 related 边。
- 当前限制：报告不是 A/B 实验，样本量和控制变量都不足；尚未接入真实 Embedding，也没有把指标写回图谱关系。

## 阶段 39：Agent 读取图谱候选反馈

- 日期：2026-08-21
- 我实现了：`read_graph_question` 的相近问题候选补充 `started_count`、`completed_count` 和 `completion_rate`；Agent 回答展示候选历史使用情况；图谱问题计划和 source 元数据保留这些字段。
- 关键链路：当前用户图谱 -> related link 反馈统计 -> `read_graph_question` 候选上下文 -> Stub/LLM 回答与 `graph_question` 计划元数据；候选仍需用户明确点击，不自动增加计划项。
- 关键取舍：复用同一条图谱和 session read model，不让 Agent 自己猜训练次数；反馈只说明入口行为，不表示掌握度、关系重要性或学习因果收益。
- 安全边界：候选和统计继续按用户、主题和当前图谱关系过滤；Agent 只能读取，不能写画像、SM-2、边权或候选计划。
- 自动化结果：暂存工程后端 `51 passed`；Python compileall、前端 typecheck 和生产构建通过；覆盖无反馈候选的 0 值元数据和已有反馈的计划保留。
- 我现在能回答：为什么 Agent 需要看到候选反馈、为什么不能让模型自己生成次数、为什么反馈只能做解释不能做排序权重、为什么计划仍不自动扩展、为什么需要复用图谱 read model。
- 当前限制：反馈只是行为统计，尚未证明候选质量或学习收益；真实 Embedding 和严格离线实验仍未接入。

## 阶段 40：图谱、Agent 与 SM-2 面试讲解稿

- 日期：2026-08-21
- 我整理了：90 秒项目介绍；从图谱到专项训练的完整请求链路；图谱、related、SM-2、Agent 的职责边界；12 个常见面试追问；现场演示顺序；必须主动说明的实现边界；关键代码定位。
- 讲解主线：事实源（题库/画像/复习/session） -> 图谱 read model -> 可验证节点入口 -> Agent 受控读取与 draft/confirm -> 训练状态机 -> 复盘写回；每一步都能指向代码和测试。
- 关键表达：图谱负责关系解释，SM-2 负责复习调度，Agent 负责上下文编排和受控行动；related 权重不等于掌握度，候选反馈不等于因果收益。
- 自动化结果：本阶段只更新学习文档，没有改代码、读取真实资料、调用真实 LLM、部署或推送 GitHub；阶段 39 的正式工程仍保持后端 `51 passed` 和本地服务可运行。
- 我现在能回答：为什么不用 Neo4j、为什么先不用真实 Embedding、Agent 是否只是固定话术、为什么只传节点 ID、如何保证用户隔离、为什么这不是多 Agent、SM-2 和图谱如何分工。
- 当前限制：讲解稿基于当前复现范围，不替代真实 LLM 联调、干净环境复现和公开仓库推送验证。

## 阶段 41：仓库发布前自检

- 日期：2026-08-21
- 我发现并修正了：README 依赖安装路径原来指向不存在的 `backend\\requirements.txt`，现在改为工程根目录的 `requirements.txt`；同时补齐暂存工程中的依赖文件镜像。
- 我实现了：`scripts/repository_preflight.py` 只读检查器，检查必需项目文件、明显密钥痕迹、本地数据库/日志/环境配置产物，并提示 Git 工作区变化；`.gitignore` 增加日志和 `.env.*` 边界。
- 关键取舍：检查器只做静态护栏，不安装依赖、不联网、不调用 LLM、不读取真实资料、不修改或删除文件；本地运行产生的产物只警告，不把“有本地 data/”误判成代码错误。
- 我现在能回答：为什么公开仓库要单独做 preflight、为什么 `.gitignore` 不能替代人工审阅、为什么自检不能证明干净环境可复现、为什么真实资料和 API Key 仍不能放进项目目录。
- 当前限制：还没有执行最终干净环境复现，也没有提交或推送 GitHub；这些工作留到项目完整后，并继续按授权单独确认。

## 阶段 42：外部 Embedding 适配器

- 日期：2026-08-21
- 我实现了：`backend/embedding.py`，包含本地确定性 Embedding、OpenAI-compatible `/embeddings` 适配器、输入/JSON/数字向量/维度校验和有界重试；`PersonalDocumentService` 继续只依赖 `EmbeddingProvider` 协议。
- 关键链路：文档分块 -> Provider `embed` -> Store 保存向量和 `embedding_mode` -> 查询向量与文档块做 cosine similarity；HTTP 细节不进入分块和检索业务。
- 测试方式：使用 `httpx.MockTransport` 检查请求 URL、Bearer 鉴权、模型和输入；覆盖 503 重试与维度变化失败；暂存工程后端完整测试为 `55 passed`，Python compileall 通过。
- 关键取舍：应用默认仍使用本地 baseline；暂不把真实外部 Provider 自动接到用户资料，避免没有明确配置和重建策略时悄悄外发个人文档或混用不同维度向量。
- 我现在能回答：为什么 Embedding 需要独立 Provider、为什么网络错误要有界重试、为什么鉴权错误不应重试、为什么维度变化必须失败、为什么外部适配器完成不等于用户级功能完成。
- 当前限制：外部 Provider 尚未接入设置页、已有文档 reindex 和真实网络联调；下一阶段处理用户配置、显式重建和 mode/维度隔离。

## 阶段 43：用户级 Embedding 配置与显式重建

- 日期：2026-08-21
- 我实现了：设置表新增 Embedding Base/Model/Key 字段；`PUT /api/settings/embedding` 支持本地或 OpenAI-compatible 模式；设置响应只返回 Key 是否存在，不返回密钥。
- 我实现了：每个文档请求按当前用户动态构造 `PersonalDocumentService`；检索先按 `embedding_mode` 隔离；新增 `POST /api/agent/documents/reindex`，只重建当前版本索引，不创建新文档版本。
- 前端变化：模型设置页增加真实 Embedding 配置表单，个人文档库增加“重建索引”按钮，并明确提示切换外部模型不会自动上传旧资料。
- 关键取舍：外部配置保存和文档 reindex 分成两个显式动作；旧本地向量在重建前不会被外部查询向量误用；真实 Embedding 仍未默认启用。
- 我现在能回答：为什么 API Key 不应出现在响应中、为什么配置必须用户隔离、为什么切换模型不能自动 reindex、为什么 reindex 不应该创建新文档版本、为什么 mode 过滤比只检查维度更可靠。
- 自动化结果：暂存工程后端 `57 passed`；Python compileall、前端 typecheck 和生产构建通过；覆盖用户级配置、Key 隔离、显式重建和旧 mode 隔离。
- 当前限制：尚未调用真实 Embedding API；下一步再用合成文档做最小网络联调，并观察供应商返回维度、延迟和错误格式。

## 阶段 44：真实 Embedding 联调门禁

- 我检查了当前运行账号的配置元数据：聊天模型已配置，但 Embedding 仍为 `demo`，没有独立的 Embedding Base、Model 和 Key；没有读取密钥内容，也没有读取或上传真实简历。
- 我新增了只读 `scripts/embedding_smoke.py`。它只读取指定用户的 Embedding 配置，只有在 `openai-compatible` 且三项配置完整时，才向 `/embeddings` 发送固定合成句；输出只包含 endpoint host、model、维度和延迟，不输出 API Key、向量值或个人数据。
- 当前账号运行结果为 `NOT_CONFIGURED`，明确显示未发起网络请求；退出码 2 表示配置门禁未满足，不表示应用故障。
- 新增回归测试覆盖 demo 模式不构造外部 Provider、未知用户不修改数据库；暂存工程该文件测试为 `2 passed`。
- 我现在能回答：为什么聊天模型和 Embedding 必须分开配置、为什么不能拿聊天 endpoint 猜测 `/embeddings`、如何证明 smoke test 没有上传真实资料、为什么真实 Embedding 不能在保存配置后自动重建旧文档。
- 当前限制：还没有真实 Embedding 的维度和延迟数据，因为当前账号没有独立 Embedding 配置；下一步只有在配置完整后再做一次合成文本网络联调。

## 阶段 45：真实 LLM 与 Personal Agent 合成联调

- 我检查了当前账号的 LLM 配置元数据：provider mode 为 `openai`，endpoint host 为 `api.deepseek.com`，model 为 `deepseek-v4-flash`，Key 已配置但没有输出密钥内容。
- 我新增了只读 `scripts/agent_llm_smoke.py`。脚本只读取 LLM 配置，不读取简历/个人文档/训练历史；它用固定合成上下文执行 `model.plan` 和 `model.answer` 两次真实调用，不写数据库、不创建 Agent 对话。
- 本次真实联调成功：规划返回并归一化了 `read_profile`、`read_due_reviews`、`read_recent_sessions` 三个已注册工具；规划耗时约 `6839.6 ms`，回答耗时约 `7868.5 ms`，回答长度 `892` 字符；脚本不输出完整回答。
- 这验证了 Agent 的两步边界：模型规划 JSON -> 后端工具白名单/写权限归一化 -> 合成工具上下文 -> grounded 回答；它不是预设固定话术，但也不是允许模型任意写数据库的自治程序。
- 新增回归测试覆盖 demo/非真实 LLM 模式不构造真实 Agent、未知用户不修改数据库；阶段 44 与阶段 45 的两个 smoke 测试均通过。
- 我现在能回答：为什么 Agent 要拆成 plan/answer 两次调用、为什么工具名必须后端白名单校验、为什么真实 LLM 返回 JSON 仍需要解析归一化、如何在不发送真实简历的情况下验证 Agent 链路。
- 当前限制：两次耗时只是本次网络观测，不是性能基准；真实 Embedding 仍未配置，Agent 文档检索仍使用本地确定性向量；还需继续验证前端失败展示和真实 LLM 异常路径。

## 阶段 46：Personal Agent 错误可观察性

- 我发现并修正了一个用户体验边界：Agent 规划或回答失败时，后端此前只返回字符串 502；前端遇到对象型 `detail` 时还会丢失服务端的 message，统一显示“请求失败”。
- 我新增 `AgentProviderError`，在 `model.plan` 和 `model.answer` 两个调用边界分别记录 `planning`、`answering` 阶段；Agent API 现在返回稳定的 `code`、`stage`、`message` 和 `retryable` 字段。
- 配置初始化失败使用 `agent_provider_error`；底层供应商异常不直接作为公共错误原文返回，避免把供应商内部细节绑定到前端协议。
- 前端 `ApiError` 会读取对象型 detail 的 message；个人 Agent 页面显示“Agent 请求未完成”、具体阶段提示和重新发送建议。
- 新增 fake provider 回归测试覆盖规划失败和回答失败；阶段 46 定向测试为 `4 passed`，没有调用真实 API、没有读取真实资料。
- 我现在能回答：为什么错误要有稳定 code、为什么规划和回答要区分、为什么工具执行失败可以记录 trace 后继续而模型回答失败要返回 502、为什么不能把供应商异常全文直接返回前端。
- 当前限制：其他模块仍保留各自错误文字；尚未处理 Agent 失败后的空会话/草稿清理和更细的前端重试按钮，下一阶段继续评估会话一致性。

## 阶段 47：Personal Agent 失败一致性

- 我发现并修正了：Agent 在模型规划前创建对话，失败时可能留下没有消息的空对话；前端失败后还会把没有持久化的乐观用户消息留在页面上。
- 我实现了：新建且没有消息、没有关联计划的空对话只在失败补偿时按 `user_id` 条件清理；已有对话不动；已经生成学习计划草稿时保留对话和草稿，并通过 `state=preserved_draft` 告知前端。
- 前端失败后移除乐观用户消息、恢复输入框文本；如果草稿已经保留，提示用户先查看草稿再重试，避免重复创建计划。
- 关键取舍：这不是把模型调用伪装成数据库事务，而是用最小状态、draft/confirm 和条件补偿降低外部模型调用带来的不一致；不能因为回答失败就删除已经产生的受控业务结果。
- 新增回归测试覆盖规划失败回滚、回答失败回滚和计划草稿保留；暂存工程后端完整测试为 `64 passed`，Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么不简单删除所有失败状态、为什么状态判定必须在后端、为什么“回滚空对话”不等于完整事务、为什么学习计划草稿要和失败回答分开处理。
- 当前限制：尚未做独立的前端自动化测试，也没有实现通用请求幂等键；工具失败仍以 trace 降级，后续继续收口演示体验和统一错误边界。

## 阶段 48：Personal Agent 工具失败降级

- 我发现并修正了：工具失败虽然会记录 trace，但原始异常可能进入前端或回答上下文；读取画像、复习队列或近期训练失败后，学习计划写工具仍可能使用不完整上下文。
- 我实现了：将工具异常归一化为 `dependency_unavailable`、`context_unavailable`、`execution_failed` 和 `write_blocked_by_context`，只把稳定摘要和 `recovery` 状态返回给前端与 Agent 上下文，不暴露内部异常原文。
- 只读工具采用部分成功策略：失败工具写入 `tool_failures`，回答继续使用其他成功上下文，并明确说明不对缺失数据下结论；Stub 和 LLM 回答提示都补充了这一约束。
- `create_learning_plan` 增加读取依赖门禁，必须成功读取 `read_profile`、`read_due_reviews` 和 `read_recent_sessions`；其中任一失败时只记录 `status=skipped`，不写入计划草稿。
- 前端 trace 显示工具是“已完成”还是“已降级”，并提示回答基于其他成功上下文继续生成。
- 新增回归测试覆盖工具异常脱敏、部分回答和写工具安全跳过；暂存工程后端完整测试为 `66 passed`，Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么只读工具可以 best-effort、为什么写工具必须有前置依赖、为什么错误码要稳定、为什么不能把缺失上下文当成空值、为什么不把原始异常直接交给前端或 LLM。
- 当前限制：只有学习计划写工具声明了依赖，尚未实现通用重试/熔断，也没有统一所有业务模块的错误协议；后续继续完善前端可恢复交互。

## 阶段 49：Personal Agent 可恢复交互

- 我实现了统一的 `submitMessage` 发送入口，表单发送和“重新发送”共享同一套乐观消息、错误回滚和状态恢复逻辑。
- Agent 失败后会移除未持久化的用户消息，把原文恢复到输入框，并显示用户主动触发的“重新发送”按钮；不会自动循环重试，避免请求风暴和重复写入。
- 当后端返回 `state=preserved_draft` 时，错误区域额外显示“加载保留草稿”；后端只对该状态返回已验证的 `conversation_id`，前端按用户作用域、对话 ID 和 `status=draft` 精确加载，不取任意最新计划。
- 加载草稿、重试和普通失败都保留 `role=alert` 的可观察提示；加载失败不会被当作成功，计划仍需原有 draft/confirm 流程确认。
- 新增/更新回归覆盖保留草稿的对话 ID 契约；暂存工程后端完整测试为 `66 passed`，Python compileall、前端 typecheck 和生产构建通过。
- 我现在能回答：为什么重试必须用户触发、为什么前端要恢复输入、为什么不能只加载最新 draft、为什么错误操作区要区分普通失败和保留草稿。
- 当前限制：尚未做独立浏览器自动化测试；下一步可以在本地用 fake provider 手动演示失败、重试、草稿加载和确认，真实 LLM 异常仍不扩展到真实资料。

## 阶段 50：本地运行态冒烟检查

- 我实现了：`scripts/local_runtime_smoke.py`，只读探测本地后端健康接口、前端开发入口，并校验 `frontend/dist/index.html` 引用的构建资源确实存在。
- 关键链路：本地 HTTP 2xx -> 不输出响应正文 -> `dist/index.html` UTF-8 读取 -> `assets/` 引用逐项存在 -> 输出稳定 PASS/FAIL 状态。
- 关键取舍：脚本不自动启动进程、不读取 SQLite、不读取真实简历或个人文档、不调用外部 API；它只负责把服务启动和构建产物两个集成边界显式化。
- 新增回归覆盖服务正常、服务不可达和构建资源缺失；测试使用合成 HTTP 响应和临时目录，不依赖正在运行的真实账号。
- 我现在能回答：为什么单元测试和前端构建之外仍需要运行态冒烟、为什么不让脚本自动启动服务、为什么先检查入口和静态资源而不是假装完成了端到端浏览器验收。
- 当前限制：尚未做独立浏览器自动化；本阶段只验证本地端口和构建产物，不代表登录、Agent 重试、计划确认等业务流程已经由自动化覆盖。

- 下一步：整理干净环境复现清单、最终演示顺序和面试讲解材料；真实资料外发、外部部署和 GitHub 提交推送仍需单独确认。

## 阶段 51：干净环境复现准备与面试演示收口

- 我实现了：`scripts/reproduction_preflight.py`，只读检查未来复现所需的核心文件、前端 `dev/typecheck/build` 脚本和 `.env`/`data`/构建产物忽略规则；不安装依赖、不启动服务、不读取数据库或个人资料。
- 我整理了：`docs/STAGE51_REPRODUCTION_DEMO_RUNBOOK.md`，固定 15 分钟演示顺序：问题边界 -> 最小主链 -> 图谱与 SM-2 -> Agent 工具链 -> 失败恢复，并为 Agent、图谱、SM-2、失败降级和项目不足准备了可复述回答。
- 关键取舍：本阶段只做复现前置准备，不把尚未执行的干净环境复现写成已完成；演示基线使用合成账号和 StubProvider，真实 LLM/Embedding 仍是独立联调边界。
- 新增回归覆盖完整合成仓库、缺失文件/前端脚本和非法 package.json/忽略规则；暂存工程后端完整测试为 `72 passed`，Python compileall、前端 typecheck 和生产构建继续通过。
- 我现在能回答：如何区分单元测试、运行态冒烟和干净环境复现；为什么 Agent 写入必须经过 draft/confirm；为什么图谱可解释规则和 Embedding 检索要分开；如何诚实描述当前未完成能力。
- 当前限制：尚未执行真实的全新目录安装和 GitHub 克隆复现，也没有独立浏览器自动化；这些工作等项目完整后再单独确认。

- 下一步：用合成数据完成一次本地人工彩排，根据实际卡点补充更细的面试讲解卡；真实资料外发、外部部署和 GitHub 提交推送仍需单独确认。

## 阶段 52：合成数据本地彩排与讲解卡

- 我实现了：`scripts/synthetic_demo_smoke.py`，用 FastAPI `TestClient`、临时合成 SQLite 和 StubProvider 串联注册、结构化简历、个人文档、五类追问卡、知识图谱、Agent draft、计划确认和逐项完成。
- 关键链路：合成账号 -> StubProvider -> 简历版本 -> 追问卡 -> RAG 高频题/画像/SM-2 图谱 -> Agent 读取工具 -> draft -> confirm -> completed；脚本只输出步骤名、状态和数量。
- 关键取舍：后端合成彩排先于浏览器人工彩排，用来证明状态链路可重复；它不是浏览器自动化，也不把本地 StubProvider 说成真实 LLM 联调。
- 我整理了：`docs/STAGE52_SYNTHETIC_DEMO_REHEARSAL.md`，包含 30 秒/90 秒项目讲解、浏览器人工操作顺序和“证据/边界”对照表。
- 新增一条集成回归覆盖八个主链步骤；暂存工程后端完整测试预期为 `73 passed`，Python compileall、前端 typecheck/build 和本地运行态门禁继续作为独立验证。
- 我现在能回答：为什么需要合成彩排、它与浏览器 E2E 的区别、简历/图谱/SM-2/Agent 如何串联、哪些结果可以展示为证据、哪些未完成能力必须主动说明。
- 当前限制：尚未实际在浏览器中完成这一轮人工彩排，仍未做全新目录复现、外部部署或 GitHub 推送。

- 下一步：按 Runbook 做一次本地浏览器人工彩排，记录页面操作卡点，再补充截图位置和个人口述版本；真实资料外发、外部部署和 GitHub 提交推送仍需单独确认。

## 阶段 53：浏览器彩排前置检查与操作卡

- 我发现并记录了：浏览器人工彩排不能假设当前登录态；自动任务不读取浏览器存储、不读取页面个人资料、不自动创建浏览器账号，因此没有把人工页面操作误报为已完成。
- 我实现了：`scripts/frontend_route_preflight.py`，只读检查 `App.tsx` 中的简历、训练、知识库、图谱、Agent 等入口，以及“重新发送”“加载保留草稿”、`role=alert` 和 Agent 错误样式标记。
- 关键取舍：静态契约检查只负责捕获入口/标记被意外删除的问题，不能代替登录、请求、页面状态更新和视觉布局验证；后端合成彩排继续作为业务状态证据。
- 新增回归覆盖完整合成前端 fixture 和缺失路由/恢复标记；暂存工程后端完整测试为 `75 passed`，Python compileall、前端 typecheck/build 通过。
- 我现在能回答：为什么不能用源码存在证明页面可用、为什么浏览器账号状态要单独确认、为什么人工彩排应使用合成账号、静态检查和 E2E 各自证明什么。
- 当前限制：尚未执行本轮浏览器人工彩排；需要用户在本地明确使用合成账号后完成点击、截图和操作记录。

- 下一步：等待并配合本地合成账号人工彩排，再把页面截图位置和个人口述版本补入讲解卡；真实资料外发、外部部署和 GitHub 提交推送仍需单独确认。

## 阶段 54：最终交付前置检查

- 我实现了：`scripts/final_delivery_preflight.py`，只读核对核心入口、阶段 40—54 文档、README 验证命令和明显密钥模式；本地日志/数据库只输出警告，不自动删除。
- 关键链路：核心文件 -> 阶段文档 -> README 测试证据 -> repository preflight 的敏感模式检查 -> 输出公开交付前的阻塞项与非阻塞本地产物警告。
- 关键取舍：最终交付检查不执行 Git commit/push、不部署、不安装依赖，也不把浏览器人工彩排、真实模型联调或全新目录复现写成已完成。
- 新增回归覆盖完整合成交付 fixture 和缺失阶段文档/README 证据；暂存工程后端完整测试预期为 `77 passed`，Python compileall、前端 typecheck/build 继续通过。
- 我现在能回答：公开仓库前置检查和业务测试的区别、为什么本地数据库只能警告不能自动删除、如何组织面试验证证据、哪些门禁必须由用户确认。
- 当前限制：浏览器人工彩排、正式全新目录复现和 GitHub 提交推送仍未完成。

- 下一步：在用户完成合成账号浏览器彩排后，依据截图和操作卡点决定最后的 UI 修正；真实资料外发、外部部署和 GitHub 提交推送仍需单独确认。

## 阶段 55：AI Agent 面试追问防守包

- 我整理了：`docs/STAGE55_INTERVIEW_DEFENSE_PACK.md`，覆盖 90 秒介绍、架构与数据边界、Agent 两步调用、工具失败降级、记忆/RAG、图谱/SM-2、失败一致性、安全、评估、工程取舍和当前不足。
- 关键方法：每个回答都尽量采用“设计选择 -> 代码/测试证据 -> 当前限制”的结构；主动区分 StubProvider、真实 LLM/Embedding、浏览器人工彩排和正式公开复现，避免把未完成能力说成完成。
- 我补充了高频追问：为什么需要 Agent、为什么规划/回答分两步、为什么写工具必须 draft/confirm、为什么图谱不是向量数据库、如何处理工具失败和 Prompt Injection、如何证明个性化有效、SQLite 和多 Agent 的边界是什么。
- 新增 `scripts/interview_pack_preflight.py` 和回归测试，只读验证防守包的关键章节存在；暂存工程后端完整测试预期为 `79 passed`，Python compileall、前端 typecheck/build 继续通过。
- 我现在能回答：如何把工程实现讲成一条可追溯的项目故事，如何在不知道或未完成时诚实作答，以及如何用测试/运行态/合成彩排支撑而不是堆砌概念。
- 当前限制：浏览器人工彩排、正式全新目录复现和 GitHub 提交推送仍未完成。

- 下一步：用户完成合成账号浏览器彩排后，用这份防守包做一次口述演练；随后再单独确认公开发布和正式复现。

## 阶段 56：简历项目表述与工程证据对齐

- 我整理了：`docs/STAGE56_RESUME_PROJECT_ENTRY.md`，包含 QTrace 的技术版/精简版简历描述、30 秒口述版本、Agent/RAG/图谱/SM-2/验证证据矩阵，以及不应夸大的能力边界。
- 关键方法：简历中的每个技术关键词都必须能够落到代码、测试、脚本或阶段文档；明确“参考 TechSpar 方向的个人独立重建”，不把项目写成官方复现，也不把本地 StubProvider、合成彩排和适配器接口写成生产效果。
- 新增 `scripts/resume_claims_preflight.py` 和测试，只读验证入口文档的核心章节及证据文档路径，不读取真实简历、个人文档、数据库或浏览器资料。
- 我现在能回答：如何把项目压缩成两三条简历 bullet、如何用证据顺序支撑 Agent 关键词、为什么 RAG/图谱/SM-2 要分开描述、如何在面试中主动说明未完成能力。
- 当前限制：浏览器人工彩排、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成；暂存与正式工程本阶段完整回归实测均为 `81 passed`，本地运行态冒烟也已通过。

- 下一步：用户在本地合成账号下完成浏览器人工彩排并确认公开发布动作；在此之前继续保持真实资料外发、外部部署和 GitHub 推送的人工确认边界。

## 阶段 57：合成浏览器演示账号种子

- 我实现了：`scripts/seed_synthetic_browser_demo.py`，只在全新 SQLite 路径中创建合成账号、StubProvider 配置、结构化简历、五类追问卡、个人文档、RAG 高频题和图谱入口，供后续浏览器人工彩排使用。
- 安全门禁：默认写入 OS 临时目录；传入 `--db` 时，目标文件及 `-wal`/`-shm`/`-journal` sidecar 必须都不存在，脚本拒绝已有路径，不覆盖、不删除，不读取现有数据库或真实资料。
- 新增回归覆盖新库登录、简历/文档/图谱上下文恢复和已有路径拒绝；脚本固定使用 StubProvider，不联网、不读取 API Key、不自动打开或登录浏览器。
- 我现在能回答：为什么要把“准备合成数据”和“操作浏览器”拆开、为什么不能直接向正式数据库写入演示账号、如何用 `REBUILD_DB_PATH` 让后端和种子数据库一致，以及为什么这仍不等于浏览器 E2E。
- 当前限制：浏览器人工彩排、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成；暂存与正式工程完整回归实测为 `83 passed`。

- 下一步：用户明确选择一个全新的合成数据库路径后，按阶段 57 说明启动后端和前端，完成一次人工浏览器彩排；其他外部动作继续保持人工确认边界。

## 阶段 58：真实 Agent Smoke 输出脱敏

- 我补强了：`scripts/agent_llm_smoke.py` 的失败输出层，现在会在输出前替换 API Key 和完整 API Base、压缩换行并限制长度；即使未来供应商异常文本格式变化，也不会直接把凭据片段打印到终端。
- 新增回归使用合成 OpenAI-compatible 配置和 fake Agent 构造器，验证异常中包含合成 Key/endpoint 时，输出不包含原文但保留 `<redacted>`；测试没有发起网络请求。
- 关键取舍：Provider 内部继续返回稳定通用错误，smoke 脚本最后再做一次防御性清理；不能把“当前异常通常不含 Key”当成永久安全保证，也不能把完整供应商错误直接回传前端或 Agent。
- 我现在能回答：为什么 Agent smoke 和 Embedding smoke 要各自做输出脱敏、为什么只保留长度受限摘要、如何在不联网的情况下测试凭据泄露边界。
- 当前限制：浏览器人工彩排、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成；暂存与正式工程完整回归实测预期为 `84 passed`。

- 下一步：用户在合成数据库上完成浏览器人工彩排；之后如需真实 LLM 联调，只使用固定合成上下文并保留本阶段脱敏边界。

## 阶段 59：回归临时产物的公开仓库边界

- 我补充了 `.gitignore` 规则，覆盖 `qtrace_stage*_pytest_tmp/`、`qtrace_stage*_formal_pytest_tmp/`、`qtrace_stage*_target_tmp/` 和 `qtrace_stage*_tmp/`，避免阶段回归目录被误加入公开仓库。
- 我更新了 `scripts/reproduction_preflight.py`，把主要 qtrace 阶段临时目录模式纳入忽略规则契约；缺少规则时复现准备会报配置错误。
- 关键取舍：忽略规则只防止误提交，不删除现有目录、不加密文件，也不等于真实资料可以放入其中；`repository_preflight.py` 继续只读报告本地产物，保留人工检查机会。
- 新增 `docs/STAGE59_REPOSITORY_ARTIFACT_IGNORES.md`，整理忽略规则、发布扫描和面试讲法；没有读取真实数据库或个人文档，没有部署、调用外部 API 或提交推送。
- 我现在能回答：为什么日常忽略和发布扫描需要两层防线、为什么不自动删除测试产物、为什么 `.gitignore` 不是隐私保护机制，以及复现前置检查如何证明本地边界配置完整。
- 当前限制：浏览器人工彩排、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成；代码行为未改变，完整回归预期仍为 `84 passed`。

- 下一步：用户在合成数据库上完成浏览器人工彩排；自动任务继续保持不删除本地产物和不执行 GitHub 操作。

## 阶段 60：隔离浏览器彩排配置前置检查

- 我实现了：`scripts/isolated_demo_preflight.py`，只读检查 `REBUILD_DB_PATH`/`REBUILD_DATA_DIR`、Vite `REBUILD_API_TARGET`、独立端口默认值，以及合成种子脚本的 `--db`/StubProvider 入口。
- 我补充了：`docs/STAGE60_ISOLATED_BROWSER_REHEARSAL.md`，把新 SQLite、后端 `8003`、前端 `5175` 和 Vite API target 组织成一套隔离启动方式；它不会复用日常 `8002/5174` 实例。
- 关键取舍：只换账号不能证明请求没有打到旧数据库，因此数据库路径、后端端口和前端代理目标都要显式隔离；静态契约仍然不能代替用户登录、点击和视觉布局验证。
- 新增两条合成回归，覆盖完整契约和缺失环境变量标记；本阶段不改业务 API、数据模型或模型调用，完整回归预期为 `86 passed`。
- 我现在能回答：为什么浏览器彩排要隔离三层边界、为什么种子脚本和浏览器人工操作分开、为什么通过源码检查仍不能声称浏览器 E2E 已完成。
- 当前限制：浏览器人工彩排、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成；没有创建合成演示库，没有读取浏览器状态，也没有调用外部 API。

- 下一步：用户明确选择一个全新的合成数据库路径后，按阶段 60 说明启动隔离后端/前端并完成浏览器人工彩排；真实资料外发、外部部署、正式复现和 GitHub 推送仍需单独确认。

## 阶段 61：合成浏览器演示端点输出

- 我补强了：`scripts/seed_synthetic_browser_demo.py` 在成功创建新 SQLite 后输出 `BACKEND_URL=http://127.0.0.1:8003`、`FRONTEND_URL=http://127.0.0.1:5175` 和 `REBUILD_API_TARGET=http://127.0.0.1:8003`，与阶段 60 的隔离启动方式保持同一份操作来源。
- 我新增了：`docs/STAGE61_SYNTHETIC_DEMO_ENDPOINT_OUTPUT.md`，说明这些端点只是本地启动提示，不是外部 API 地址，不包含 API Key，也不会自动启动服务或打开浏览器。
- 关键取舍：把端点打印出来可以减少“新数据库却代理到旧后端”的人工抄写错误，但不能代替环境变量生效检查、后端健康检查或浏览器 E2E。
- 新增一条回归，使用 fake seed 结果验证输出契约，不创建额外数据库；暂存/正式工程完整回归预期为 `87 passed`。
- 我现在能回答：为什么数据种子脚本可以输出本地端点、为什么仍要保留后端/前端分离启动、为什么端点输出不是部署能力或浏览器验收证据。
- 当前限制：浏览器人工彩排、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成；没有读取浏览器状态、真实资料或 API Key。

- 下一步：用户确认全新的合成数据库路径后，按脚本输出启动隔离后端/前端并完成人工浏览器彩排；真实资料外发、外部部署、正式复现和 GitHub 推送仍需单独确认。

## 阶段 62：隔离合成环境运行态冒烟

- 我实际启动了一套全新的合成 SQLite、临时后端和临时前端，并复用 `scripts/local_runtime_smoke.py` 检查隔离运行态；健康接口、前端入口和 `dist` 资源均通过。
- 第一次发现推荐端口 `5175` 被现有 node 进程占用，备用 `5176` 也被占用；我只读确认端口状态，没有停止未知进程，最终使用 `8004/5177` 完成验证。
- 为了让端口冲突时的操作可重复，我给 `seed_synthetic_browser_demo.py` 增加了 `--backend-port`/`--frontend-port` 和 `1—65535` 范围校验，脚本会把自定义端口写入三项端点输出；新增回归覆盖备用端口输出。
- 新增 `docs/STAGE62_ISOLATED_RUNTIME_SMOKE.md`，记录运行态命令、端口占用处理、实际验证结果和面试讲解边界；完整回归预期为 `88 passed`。
- 关键取舍：运行态冒烟补足“真实进程和 HTTP 入口是否连通”的证据，但不读取响应正文、不接触真实资料、不自动关闭未知服务，也不把它说成浏览器 E2E。
- 我现在能回答：为什么单元测试之外要做运行态冒烟、为什么端口参数要可配置、为什么自动任务不能为了清端口而杀掉未知进程。
- 当前限制：浏览器人工彩排、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成；验证生成的合成数据库没有被自动删除。

- 下一步：用户确认全新的合成数据库路径和空闲端口后，按脚本输出启动隔离服务并人工完成浏览器彩排；真实资料外发、真实模型联调、外部部署、正式复现和 GitHub 推送仍需单独确认。

## 阶段 63：端口校验先于合成数据创建

- 我补充了 `seed_synthetic_browser_demo.py` 的副作用顺序回归：当 `--backend-port 0` 时，先返回失败，不调用 `seed_browser_demo`，因此不会创建 SQLite 或写入合成账号上下文。
- 新增 `docs/STAGE63_PORT_VALIDATION_BEFORE_SEED.md`，把“端口值域校验”和“端口是否被占用”分成两层：脚本负责前者，启动前检查/用户负责后者；自动任务不停止未知进程。
- 关键取舍：即使是合成演示，也应让参数校验发生在有副作用的操作之前，避免错误命令留下半成品数据库；真实资料场景更不能依赖事后清理。
- 新增一条回归；暂存/正式工程完整回归预期为 `89 passed`，前端构建、合成主链和隔离运行态证据保持不变。
- 我现在能回答：如何证明非法输入没有触发数据写入、为什么“返回错误”不等于“没有副作用”，以及端口值域与端口占用为什么要由不同层负责。
- 当前限制：浏览器人工彩排、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成；没有读取真实资料或 API Key。

- 下一步：用户确认全新的合成数据库和空闲端口后，按阶段 62/63 命令启动隔离服务并人工完成浏览器彩排；真实资料外发、外部部署、正式复现和 GitHub 推送仍需单独确认。

## 阶段 64：合成浏览器认证入口检查

- 我在隔离的合成浏览器环境中实际打开 QTrace，确认登录页和注册页的标题、邮箱/密码输入框、登录/注册按钮以及注册表单可以渲染；没有读取现有浏览器 cookie/localStorage。
- 浏览器安全规则要求在页面中输入密码前临时确认，因此自动任务没有代填合成密码、提交登录或声称完整浏览器 E2E 已完成；临时后端/前端进程已停止，已有 `5175/5176` node 进程未被触碰。
- 我扩展了 `scripts/frontend_route_preflight.py` 的 `REQUIRED_AUTH_MARKERS`，并新增回归覆盖认证入口完整和缺失两种情况；新增 `docs/STAGE64_SYNTHETIC_BROWSER_ENTRY_CHECK.md` 记录页面证据和边界。
- 暂存/正式工程完整回归预期为 `90 passed`，前端构建、合成主链和运行态检查继续通过。
- 我现在能回答：为什么 DOM 页面可见仍不能证明密码提交成功、为什么认证入口可以做静态契约、为什么自动化不能在未确认时输入密码，以及如何区分页面渲染、认证请求和完整 E2E 证据。
- 当前限制：登录提交、业务页面人工点击、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成；没有读取真实资料或 API Key。

- 下一步：用户确认合成账号密码输入后，按阶段 62/64 完成人工登录和页面彩排；真实资料外发、真实模型联调、外部部署、正式复现和 GitHub 推送仍需单独确认。

## 阶段 65：前端认证客户端边界

- 我补充了：`scripts/frontend_route_preflight.py` 对 `frontend/src/api.ts` 的只读认证客户端契约检查，覆盖统一 `/api` 前缀、条件式 `Authorization: Bearer`、`/auth/${mode}` 登录/注册请求，以及前端请求层的存在性。
- 我新增了：`docs/STAGE65_AUTH_CLIENT_BOUNDARY.md`，把页面入口、认证请求源码、token 持久化和完整浏览器 E2E 分成不同证据层级，补充了面试中的 token 请求链路回答。
- 新增一条回归，验证认证客户端标记缺失时会被报告；正式交付前置检查同时把 `frontend/src/api.ts` 和阶段 65 文档纳入必需项。
- 关键取舍：认证头集中在 `apiFetch`，组件只显式传入 token；源码契约可以防止公共请求层被意外删改，但不能证明密码提交、token 持久化、过期刷新、供应商鉴权或真实浏览器请求成功。
- 暂存/正式工程完整回归预期为 `91 passed`；没有读取 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或提交推送 GitHub。
- 正式工程第一次使用默认 pytest 临时目录时，Windows 对系统临时目录返回拒绝访问，错误发生在 `tmp_path` fixture setup，不是业务断言失败；改用全新的 `qtrace_stage65_formal_pytest_tmp` 作为 `--basetemp` 后正式回归为 `91 passed`。这个绕行和后续处理方式已记录在 `docs/STAGE65_AUTH_CLIENT_BOUNDARY.md`，没有删除原临时目录。
- 我现在能回答：登录/注册为什么不带 token、业务请求的 Bearer token 如何统一注入、为什么 `FormData` 不能强行设置 JSON Content-Type，以及为什么这些源码证据仍不足以声称登录 E2E 完成。
- 当前限制：登录提交、业务页面人工点击、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成。

- 下一步：用户确认合成账号密码输入后，按阶段 62/64/65 完成人工登录和页面彩排；真实资料外发、真实模型联调、外部部署、正式复现和 GitHub 推送仍需单独确认。

## 阶段 66：前端认证状态生命周期

- 我补充了：`scripts/frontend_route_preflight.py` 对 `frontend/src/App.tsx` 的认证状态生命周期契约，覆盖启动读取 token、认证成功保存 token、账户校验失败清除状态、主动退出清除状态，以及 `token + user` 不完整时回到登录入口。
- 我新增了：`docs/STAGE66_AUTH_STATE_LIFECYCLE.md`，解释刷新页面、过期 token、退出登录和业务路由门禁的状态链，并记录本地 localStorage 方案与生产 HttpOnly Cookie/XSS/CSRF 防护之间的取舍。
- 新增一条回归，验证认证状态生命周期标记缺失时会被报告；正式交付前置检查同时把阶段 66 文档纳入必需项。
- 关键取舍：本地学习项目优先保持刷新后可恢复的简单体验，但主动说明 localStorage 暴露于 XSS 的风险；不能把源码契约说成生产级认证安全、过期刷新或完整浏览器 E2E。
- 暂存/正式工程完整回归预期为 `92 passed`；本阶段没有读取 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或提交推送 GitHub。
- 我现在能回答：刷新页面时 token 如何恢复、账户校验失败为什么要清除本地状态、为什么业务路由要等待 `token + user` 完整，以及生产环境为什么不能直接照搬 localStorage。
- 当前限制：登录提交、业务页面人工点击、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成。

- 下一步：用户确认合成账号密码输入后，按阶段 62/64/65/66 完成人工登录和页面彩排；真实资料外发、真实模型联调、外部部署、正式复现和 GitHub 推送仍需单独确认。

## 阶段 67：认证过期统一回退

- 我发现并修复了一个实际状态缺口：用户已进入业务页面后，如果带 token 的请求返回 401，原来的 `apiFetch` 只抛错，不会统一清除根组件中的认证状态。
- 我实现了：`apiFetch` 对带 token 的 401 派发 `AUTH_EXPIRED_EVENT`，`App.tsx` 监听后复用 `clearAuthState()` 清除 token、user、settings，并回到登录入口；当前请求仍抛出 `ApiError`，不吞掉业务错误处理。
- 我新增了：`docs/STAGE67_AUTH_EXPIRY_RECOVERY.md`，解释请求层与 React 状态层如何解耦，以及当前方案没有覆盖 token 刷新、并发去重、跨标签页同步和生产认证安全。
- `scripts/frontend_route_preflight.py` 新增认证过期事件和状态监听契约，回归覆盖缺失事件回退标记；正式交付前置检查同时把阶段 67 文档纳入必需项。
- 暂存/正式工程完整回归预期为 `93 passed`；前端 typecheck/build、认证状态预检继续通过。本阶段没有读取 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或提交推送 GitHub。
- 我现在能回答：页面停留期间 token 过期怎么办、为什么事件由请求层发出而由根组件清理状态、为什么当前请求仍要保留 `ApiError`，以及为什么这不是 token 刷新机制。
- 当前限制：登录提交、业务页面人工点击、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成。

- 下一步：用户确认合成账号密码输入后，按阶段 62/64/65/66/67 完成人工登录和页面彩排；真实资料外发、真实模型联调、外部部署、正式复现和 GitHub 推送仍需单独确认。

## 阶段 68：画像待复习项直达训练

- 我发现并修复了一个与项目核心目标直接相关的 UX 缺口：画像页能够展示 SM-2 到期复习项，但过去没有直接进入专项训练的入口，个性化结果停留在展示层。
- 我实现了：`ProfilePage` 为每个待复习项增加“立即复习”，通过 `buildDueReviewPath` 把 `topic`（若有）和 `focus` 复习点带入 `/topic-drill`；`TopicDrillPage` 继续复用既有 query 参数，把它们传给后端 `mode=topic_drill`。
- 我新增了：`docs/STAGE68_REVIEW_QUEUE_ENTRY.md`，记录画像→专项训练的参数链、后端校验边界和面试讲法；没有复制 SM-2 逻辑或新增数据表。
- `scripts/frontend_route_preflight.py` 新增 `REQUIRED_REVIEW_FLOW_MARKERS`，回归覆盖待复习入口缺失；正式交付前置检查同时把阶段 68 文档纳入必需项。
- 暂存/正式工程完整回归预期为 `94 passed`；前端 typecheck/build、认证预检和交付检查继续通过。本阶段没有读取 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或提交推送 GitHub。
- 我现在能回答：为什么画像不应只是报表、SM-2 到期项如何成为下一场训练的 focus、为什么前端只传参数而后端仍需重新校验，以及没有 topic 的通用复习点如何处理。
- 当前限制：登录提交、业务页面人工点击、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成。

- 下一步：用户确认合成账号密码输入后，按阶段 62/64/65/66/67/68 完成人工登录和画像→专项训练→Agent 彩排；真实资料外发、真实模型联调、外部部署、正式复现和 GitHub 推送仍需单独确认。

## 阶段 69：画像页可恢复加载

- 我发现并修复了一个画像页稳定性缺口：`/profile` 或 `/topics` 请求失败时，原页面会一直显示“正在读取画像…”，没有错误解释和重试入口。
- 我实现了：`ProfilePage` 增加 `loadError`/`loadKey` 状态，加载失败时展示 `role="alert"` 错误卡和“重新加载画像”按钮；重试只重新读取同一组接口，不修改 token、画像或学习计划。
- 阶段 68 的 SM-2 到期项“立即复习”入口保留不变，成功加载后仍可将 `topic/focus` 带入专项训练。
- 我新增了：`docs/STAGE69_PROFILE_RECOVERY.md`，记录 loading/失败/成功三态、可观察错误和无副作用重试边界；`scripts/frontend_route_preflight.py` 与回归新增画像恢复标记检查。
- 暂存/正式工程完整回归预期为 `95 passed`；前端 typecheck/build、画像链路预检和交付检查继续通过。本阶段没有读取 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或提交推送 GitHub。
- 我现在能回答：页面请求失败时为什么不能渲染空画像、为什么要把错误状态显式展示、重试如何保证不产生副作用，以及如何保持画像到专项训练的已有链路。
- 当前限制：登录提交、业务页面人工点击、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成。

- 下一步：用户确认合成账号密码输入后，按阶段 62/64/65/66/67/68/69 完成人工登录、画像加载/重试、画像→专项训练→Agent 彩排；真实资料外发、真实模型联调、外部部署、正式复现和 GitHub 推送仍需单独确认。

## 阶段 70：专项训练页可恢复加载

- 我发现并修复了一个专项训练页的状态缺口：训练领域、SM-2 到期复习项或可选图谱问题读取失败时，原页面会把空对象继续渲染成“还没有训练领域”，用户无法区分真正的空数据和请求失败。
- 我实现了：`TopicDrillPage` 增加 `loading`/`loadKey`，把初始读取拆成 loading、成功和失败三态；失败时清空当前读模型并展示 `role="alert"` 错误卡和“重新加载训练领域”按钮，重试只重新读取，不创建 session、不修改画像或 SM-2。
- 我补充了卸载保护：异步读取完成前如果用户离开页面，`active` 标记阻止旧请求覆盖新页面状态；阶段 68 的 `topic/focus`、计划、追问卡和图谱入口参数没有改变，真正开始训练仍由后端重新校验。
- 我新增了：`docs/STAGE70_TOPIC_DRILL_RECOVERY.md`、前端恢复契约和回归测试；最终交付预检将阶段 70 文档纳入必需项。
- 暂存/正式工程完整回归目标为 `96 passed`；本阶段只使用合成源码夹具，没有读取 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或提交推送 GitHub。
- 我现在能回答：为什么失败不能渲染成空列表、为什么重试入口应只触发读请求、为什么前端 `active` 标记能防止卸载后的旧异步结果污染状态，以及为什么前端恢复提示仍不是后端授权边界。
- 当前限制：登录提交、业务页面人工点击、正式全新目录复现、公开发布和 GitHub 提交推送仍未完成；恢复契约不等于离线缓存、请求取消、退避重试或生产级 token 刷新。

- 下一步：用户确认合成账号密码输入后，按阶段 62/64/65/66/67/68/69/70 完成人工登录、画像/专项训练加载恢复、画像→专项训练→Agent draft/confirm 彩排；真实资料外发、真实模型联调、外部部署、正式复现和 GitHub 推送仍需单独确认。

## 阶段 71：本地语义 Embedding

- 我实现了 `LocalSentenceTransformerEmbeddingProvider`，把“下载到本机的语义模型”和已有的确定性向量、远程 Embedding 分成三个清晰模式；Provider 懒加载 `sentence_transformers`，并强制 `local_files_only=True`，缺少依赖或模型文件时显式失败，不偷偷联网。
- 我新增了 `settings.embedding_model_path`、旧 SQLite 的迁移、`PUT /api/settings/embedding` 的 `local-model` 分支，以及设置页的“本地语义模型”入口；模型目录不会与远程 API Base、Model、Key 混用，也不会在接口响应中回传 Key。
- 我保留了显式重建原则：切换模式不会自动重写已有 chunks，检索先按 `embedding_mode` 隔离，用户点击“重建索引”后才用新 Provider 生成当前版本向量。
- 我新增 `requirements-local-embedding.txt`，基础安装不强制引入重量级模型依赖；新增本地 Provider 契约、API/索引切换、只读 Smoke 和前端设置标记回归，测试只用 fake loader、合成目录和合成文本。
- `scripts/embedding_smoke.py` 现在兼容旧数据库的缺失路径列；对 `local-model` 只调用固定合成句，成功输出 `network=disabled`，不会访问网络或读取个人文档。
- 我现在能回答：为什么 Embedding 不等同于 LLM、为什么要保留 deterministic baseline、为什么本地模型必须 `local_files_only`、为什么模型切换后要显式 reindex，以及为什么契约通过还不能证明语义质量更好。
- 当前验收：只读定位到 Hugging Face 缓存中的中文 `shibing624/text2vec-base-chinese` 和英文 `all-MiniLM-L6-v2`，在当前 E:\\Anaconda 环境安装可选依赖后，用中文模型和全新合成 SQLite 完成真实离线 Smoke，输出 768 维向量并确认 `network=disabled`；没有读取真实简历或个人文档。
- 本阶段没有调用外部 API、读取或输出 API Key、删除文件、部署或提交推送 GitHub。

- 下一步：建立固定的合成查询/相关文档标注集，对 deterministic 与本地语义模型比较 Recall@K、MRR 等离线检索指标；若更换模型，重复显式 reindex 和 Smoke。

## 阶段 72：模型设置工业化前端收口

- 我先保存了前端优化前的本地快照，快照排除了 `.git`、`.env`、数据库、日志、构建产物和阶段临时目录；没有用 Git 提交污染当前工作区，也没有推送远程仓库。
- 我修复了设置页的一个真实可观察性缺口：原来 LLM 和 Embedding 共用 `busy/message/error`，导致 Embedding 失败可能显示在 LLM 面板；现在两条链路分别维护状态、按钮忙碌态和 `role="alert"`/`role="status"` 反馈。
- 我在 `frontend/src/api.ts` 增加结构化错误格式化，将 FastAPI 校验数组转换为字段位置和错误消息，保留定位信息但不输出请求体、API Key 或供应商原始凭据。
- 我按浅色工业瑞士印刷方向局部改造模型设置页：米白纸张、碳黑硬边、单一警示红、直角输入和按钮、等宽技术标签、窄屏适配与 reduced-motion 保护；新样式限定在 `.settings-page`，不改变其他业务页面的全局视觉和路由。
- 界面写清两条验证路径：LLM 保存后要到个人 Agent 用合成问题验证真实调用；Embedding 保存后要到个人文档库显式重建索引，再看检索证据。保存状态不能被误说成模型调用成功。
- 新增 `REQUIRED_SETTINGS_FEEDBACK_MARKERS` 和回归测试，锁定两组状态、结构化错误格式化函数和反馈样式；暂存前端 `npm run typecheck`、`npm run build`、源码预检通过，设置契约回归为 `11 passed`。
- 浏览器自动化只能看到当前登录页，当前会话没有可复用的登录标签；我没有输入密码、创建账号或读取 localStorage，因此登录后的视觉验收仍保留为用户使用合成账号执行的清单。
- 本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或提交推送 GitHub。

## 阶段 76：minimalist-ui 浅色主题

- 我把视觉主题做成共享业务组件之上的可切换层：深色 Tactical Telemetry 保持默认，新增 `minimalist-ui` 浅色主题，主题选择写入浏览器本地 `localStorage`，不触碰后端状态。
- 我新增 `ThemeToggle`，在登录页、模型初始化页和工作区侧栏提供 `DARK/LIGHT` 入口；根节点的 `data-theme` 属性驱动 CSS，现有路由、认证状态、LLM、Embedding、Agent、SM-2 和个人文档请求不变。
- 浅色 token 采用暖白画布、白色面板、炭黑正文、`#EAEAEA` 细边界和低饱和语义色；标题使用中文衬线回退链，正文使用系统无衬线，元数据使用等宽字体；不使用渐变、霓虹、玻璃效果或厚重阴影。
- 首页、导航、表单、常见列表、状态反馈和模型设置页均增加浅色覆盖规则，避免只换背景色而残留深色终端的扫描线、黄色大面积按钮或低对比度文字。
- 改造前使用严格白名单创建 `qtrace-pre-minimalist-theme-20260822-142528.zip`，SHA256 为 `17144E47F053B909CEA18290B3F7417942B70195E770BE98759C55CE8BB59295`；快照没有包含环境文件、数据库、个人资料、日志、构建产物或依赖目录。
- 暂存工程 typecheck、build、主题源码预检通过，前端预检回归为 `14 passed`；正式工程同步后的全量回归和登录后浏览器双色视觉验收仍未宣称完成。
- 用户继续验收发现深色首页展示标题也可能在窄卡片中溢出；我将深色与浅色主题的展示标题统一调整为卡片友好的响应式字号、字距和宽度约束，保留两套主题各自的字形与配色，不改变路由或业务逻辑；修正后暂存 typecheck、build 和源码预检继续通过。
- 本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或提交推送 GitHub。

- 下一步：同步正式工程，执行正式全量回归、前端 build、源码预检和本地 runtime smoke；用户随后在已登录页面切换 DARK/LIGHT，确认首页、模型设置和窄屏下的两套视觉。

## 阶段 75：前端 Tactical Telemetry 重设计

- 我修正了前一轮的方向误判：之前只把 industrial-brutalist-ui 解释成浅色 Swiss Industrial Print，并且只改了模型设置页；本阶段根据用户参考图选择深色 Tactical Telemetry / CRT Terminal，重做整个前端应用壳层和统一视觉 token。
- WorkspaceLayout 增加现场系统式左侧导航、路由分组、顶部 ROUTE ENGINE 状态栏、用户节点状态和底部系统标语；所有既有路由、认证状态、Outlet 和后端 API 保持不变。
- styles.css 新增深色基底、暖白前景、黄色主信号色、橙红错误色、硬边面板、无圆角/阴影、网格与扫描线纹理、巨型显示字和等宽技术标签；首页改成左侧 TRACE / YOUR NEXT / ANSWER. 主构图加右侧训练输入面板。
- 响应式断点把侧栏、首页双栏、阶段列表和设置面板收束到窄屏；全局焦点、loading、error、success、disabled 和 reduced-motion 规则保留。
- 改造前快照为 qtrace-pre-telemetry-redesign-20260822-140204.zip，SHA256 为 5C9F2FFFA14A7663F4BA36378814DD1EC314EB33E24D4B548B21AF9DABBD2620；没有使用 Git 提交代替快照，也没有删除文件。
- frontend_route_preflight.py 新增终端视觉契约，正式工程前端 typecheck/build、13 条前端预检回归、108 条全量回归和本地运行态 smoke 通过。
- 本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或提交推送 GitHub；已登录浏览器的最终视觉验收仍需用户使用合成账号完成。

- 下一步：同步正式工程文档并执行正式工程全量回归、构建和运行态检查；用户回来后按阶段 72 清单验收模型设置页。

## 阶段 73：模型设置加载恢复

- 我发现阶段 72 之后仍有一个可观察性缺口：`/settings` 初始化请求没有错误分支，后端未启动、认证过期或接口异常时页面会永久显示 loading。
- 我为 `SettingsPage` 增加 loading、failure、success 三态；失败时显示安全错误详情和“重新读取模型设置”按钮，重试只重新读取，不保存配置、不创建索引、不调用模型。
- 初始化请求增加 `active` 卸载保护，避免用户切换路由后旧响应覆盖新页面；loading 用 `role="status"`，失败卡用 `role="alert"`，并沿用工业瑞士印刷硬边样式。
- 这进一步明确了本地模型的证据层级：设置可读、配置已保存、文档重建索引、检索返回证据分别证明不同事情，页面“已配置”不等于模型推理已经成功。
- 新增设置加载恢复源码契约，暂存前端 typecheck/build、源码预检和相关回归通过，回归为 `12 passed`。
- 本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或提交推送 GitHub。

- 下一步：同步正式工程，执行全量回归和运行态 smoke；随后停止本轮约两小时的安排任务，保留用户登录后的视觉验收清单。

## 阶段 74：Embedding 离线检索评估

- 我新增 scripts/embedding_eval.py，用固定的四份合成技术文档、四个查询和人工相关文档 ID，对 local-deterministic 和可选 local-model Provider 统一计算 Recall@K、MRR、查询数和向量维度；评估不读数据库、不读真实资料，也不写入个人文档索引。
- 我保留本地模型的离线边界：只有显式传入已下载模型目录时才加载 Sentence-Transformers，Provider 内部继续强制 local_files_only=True；未传目录时只跑默认确定性基线，缺失目录直接失败，不尝试远程下载。
- 当前固定合成集上，确定性基线为 dimension=128、Recall@2=1.000、MRR=1.000；已下载中文模型为 dimension=768、Recall@2=1.000、MRR=1.000，命令输出 network=disabled。
- 我新增 tests/test_embedding_eval.py，覆盖指标计算、稳定排序、默认基线和缺失模型目录的无网络失败路径；这一步把“模型能返回向量”和“在小型标注集上检索命中”区分开。
- 我现在能回答：Recall@K 与 MRR 分别衡量什么、为什么要固定人工相关文档 ID、为什么小样本 1.0 不能说明真实简历质量，以及为什么离线评估不能替代显式 reindex 和浏览器人工验收。
- 本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或提交推送 GitHub。

## 阶段 77：TechSpar-informed 产品工作台

- 我先对本地 `techspar` 做只读前端审计，确认它的流畅感主要来自统一 AppShell、分组导航、模式选择后一主操作、稳定页面节奏、skeleton/empty/error 状态和桌面/移动导航差异，而不是某一套颜色或组件库。
- 我重做了 QTrace 的 `WorkspaceLayout`：导航由 `NAV_GROUPS` 统一维护；桌面侧栏支持 `qtrace_sidebar_collapsed` 收起偏好；窄屏变成菜单按钮、抽屉和遮罩；路由变化会关闭移动抽屉。没有改变路由、认证、Outlet 或 API。
- 我重做了首页入口：用 `TRAINING_MODES` 把简历模拟、专项训练、JD 定向、录音复盘做成选择卡；用户选择后只看到当前模式的一主操作。原有简历 PDF 上传、摘要和 `/interview/start` 请求保持不变，其他模式只跳转到已有路由。
- 首页简历状态增加可见 skeleton，上传/启动错误使用 `role="alert"`；训练卡增加 selected、hover、focus-visible、窄屏单列和 reduced-motion 处理。
- 我没有直接引入 TechSpar 的 Tailwind、Radix、Framer Motion 或图标依赖，而是用现有 React/CSS 实现相同的工作流结构，控制复现成本和变更范围。
- 新增 `docs/STAGE77_TECHSPAR_INFORMED_PRODUCT_UX.md`、产品 UX 静态预检契约和回归；暂存前端 typecheck/build、`frontend_route_preflight.py` 和前端预检 `14 passed` 已通过。
- 改造前严格白名单快照为 `qtrace-pre-techspar-ux-20260822-144831.zip`，SHA256 为 `4FC3E7C3D53C703A5804D31DF474733805BC2A9E894336879D88D292E2A10BCE`；没有包含 `.env`、API Key、数据库、真实资料、日志、依赖或构建产物。
- 我现在能回答：为什么首页要先选模式、为什么 UI 偏好放在 localStorage、为什么参考交互规律而不复制依赖，以及为什么源码预检不能替代登录后的浏览器视觉验收。
- 正式工程验证随后完成：前端 typecheck/build、源码预检、前端契约回归 `14 passed`、使用全新合成 SQLite 和显式 basetemp 的全量回归 `109 passed`、`local_runtime_smoke.py` 和 `final_delivery_preflight.py` 均通过；交付预检只提示 15 个本地日志/数据等产物待人工审阅，没有发现明显密钥模式。当前仍需登录后的桌面/窄屏双色主题人工验收和 GitHub 提交推送确认；本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件或部署。

## 阶段 78：工程总笔记与面试主线

- 我把之前分散的多阶段实现收束为 `docs/QTRACE_ENGINEERING_NOTE.md`，按“用户问题 -> 数据流 -> 分层职责 -> 失败边界 -> 验证证据”组织，而不是继续按页面数量罗列功能。
- 总笔记固定一条面试主线：结构化简历 -> 项目追问卡 -> 面试 session/状态机 -> 复盘写回画像与 SM-2 -> Personal Agent 读取到期项 -> draft/confirm 计划 -> 专项训练。
- 总笔记补充 React/FastAPI/SQLite/Provider/Embedding/知识图谱的职责边界、Agent 工具白名单、RAG 的显式 reindex、本地模型 `local_files_only`、当前 109 条全量回归证据和未完成边界。
- 总笔记包含三分钟项目介绍和十个高频追问速答，后续学习以复述和演示这条主线为主，不再无目标地继续堆页面。
- 正式工程交付预检已把总笔记纳入必需文件；笔记已同步到 Obsidian 的 `QTrace-工程总笔记.md`，阶段总结也补充了本阶段入口。
- 本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或提交推送 GitHub。

## 阶段 79：浅色产品化工作台

- 我把“参考 TechSpar”具体化为交互结构，而不是照搬页面：统一 AppShell、分组导航、模式选择后一主操作、明确的 loading/empty/error 状态，以及桌面/移动导航差异。
- 我新增 `frontend/src/components/ProductUI.tsx`，用 `PageHeader`、`Surface`、`StatusBadge` 和 `StatePanel` 形成可复用的表现层基础；这些组件不持有业务状态，不改变 API。
- 我新增 `frontend/src/product.css`，为浅色主题建立工作台 token：暖灰画布、白色工作表、碳黑正文、砖红信号色和低饱和蓝/绿语义色；保留深色主题作为可选模式，不使用渐变和厚重阴影。
- 我把 `WorkspaceLayout` 接到新的浅色工作台壳层，并迁移“开始训练”“我的画像”“个人 Agent”三条核心路径；其他旧页面通过工作台级样式统一输入、标题、表面和按钮，后续可以继续按组件层逐页迁移。
- 我把新的组件文件、样式文件和挂载点加入 `frontend_route_preflight.py` 的静态契约。暂存工程 typecheck/build、前端预检和 `14 passed` 回归通过；本地独立暂存前端已确认浅色登录入口能够渲染，登录后工作区和真实请求仍需合成账号人工验收。
- 改造前源码白名单快照为 `qtrace-pre-light-product-workspace-20260822-151302.zip`，SHA256 为 `0A1C3C9E9ED64F87241051629460102A3CE0A07E3EECE2E3727C5973E0833D06`；没有读取或输出 API Key、真实资料或浏览器存储，没有外部部署和 GitHub 推送。

- 正式工程同步后，typecheck/build、源码预检、前端契约回归 `14 passed`、全量合成回归 `109 passed`、`local_runtime_smoke.py` 和 `final_delivery_preflight.py` 均通过；runtime 第一次使用了错误的后端 URL，修正到 `/api/health` 后通过，契约回归第一次遇到系统临时目录权限问题，改用项目范围 basetemp 后通过。
- 下一步：用户验收核心路径和双色主题后，再继续迁移剩余页面；GitHub 提交/推送、公开部署和真实资料复现仍需单独确认。

## 阶段 81：QTrace 品牌图标挂载

- 复用已有 `frontend/public/qtrace-icon.png`，在 `WorkspaceLayout` 左上角用图片替换 `QT` 文字方块；品牌文字仍保留为文本，避免图标承担全部语义。
- CSS 只负责固定图标尺寸、裁切和收起侧栏时的显示策略，没有修改业务布局、认证、路由或 API。
- 在 `frontend_route_preflight.py` 增加图标路径静态标记，形成“资源存在 + JSX 挂载 + 构建通过”的最小防回归链路。
- 正式工程 typecheck、build、前端预检及 `14 passed` 前端契约回归均通过；浏览器双色主题的尺寸观感仍等待人工验收。

## 阶段 82：TechSpar 前端迁移

- 用户明确要求不再保留 QTrace 原有前端，因此我把 TechSpar 的 275 个源码文件作为正式活动 UI 基线，逐文件按相对路径合并；旧 UI 的 4 个专属文件和一次错误嵌套拷贝都移动到 `frontend/migration_leftovers`，没有删除。
- QTrace-specific 改动集中在 `interview.ts`/`personalAgent.ts` API adapter、QTrace 图标和品牌文案。复用的是成熟的信息架构、AppShell、导航、面试/复盘/画像/Agent 页面和交互节奏，不是把 QTrace 旧页面继续套皮。
- 新增 `scripts/techspar_frontend_preflight.py` 与 2 条回归，检查活动源码集合、路由、品牌、QTrace API 适配和旧 UI 不在活动源码树；相对 TechSpar 的文件集合预检通过。
- 许可边界：TechSpar 为 CC BY-NC 4.0，`THIRD_PARTY_NOTICES.md` 保留原项目链接、许可证链接、改动说明和非商业限制；未经原作者许可不作商业用途。
- 证据：前端 typecheck/build 通过；全量 Python 回归 `113 passed`；5174 页面、`/api/health` 代理和 qtrace-icon 资源本地返回 200。pytest 需要显式项目范围 `--basetemp`，因为系统临时目录已有 WinError 5 权限问题。
- 我现在能回答：为什么采用“TechSpar UI + QTrace adapter”而不是继续修旧 UI；为什么接口不一致时要显式降级；为什么同步回答不能伪装成流式；以及为什么源码预检和构建仍不能代替登录后的浏览器视觉验收。

## 阶段 83：Vite 开发运行时黑屏恢复

- 用户反馈 5174 页面全黑。我先用浏览器 DOM 和控制台检查，而不是把问题误判成后端未启动；HTTP 200 只能证明服务器返回了 HTML，不能证明 React 已挂载。
- 根因是 Vite 8 自动注入的 `/@vite/client` 在当前运行时引用未定义的 `__BUNDLED_DEV__`，开发客户端异常后 React 入口没有继续执行。
- 我在 `frontend/vite.config.js` 中关闭 HMR，并增加 post `transformIndexHtml` 插件移除 `/@vite/client`。这是开发服务器兼容性修复，不改变 `/api`/`/ws` 代理、业务 API 或生产构建；代价是开发时需要手动刷新。
- 修复后的 5174 HTML 不再包含 Vite/React Refresh 开发注入，浏览器 DOM 已出现 QTrace 首页内容；`npm run typecheck` 和 `npm run build` 均通过，只有原有大型 bundle 提示。
- 这一步让我能解释“页面黑屏如何分层定位”：先看 HTTP，再看 HTML 注入，再看控制台入口异常，最后用 DOM/截图确认渲染；不能只凭地址栏的 200 判断页面健康。

本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或 GitHub commit/push。

## 阶段 84：移除启动动画并直达登录

- 我先在代码修改前创建了不包含 `.env`、数据库、日志、依赖和构建产物的工程快照：`C:\Users\clearsnowsong\Documents\ChatGPT\秋招\techsnowsong_stage\snapshots\qtrace-pre-login-entry-and-frontend-dedup-20260822-222113.zip`，SHA256 为 `E29D70156BBD3E03722382E46D1BC20BED4FC6798C73C99E065425CE9F58F7B7`。
- 用户未登录访问 `/` 时，`App.tsx` 现在直接跳转 `/login`；已有 token 访问 `/` 或 `/login` 直接进入 `/profile`，不再经过 Landing 动画。
- 我移除了 `App.tsx` 对 `Landing` 的活动 import，而不是删除文件；这样保留可恢复材料，同时启动视频不再进入活动路由和首屏依赖。
- 新增 `qtrace_entry_preflight.py` 和两条回归，防止 Landing/`hero-intro` 重新成为活动入口。专项回归 `2 passed`，typecheck/build 通过，浏览器 5174 已验证未登录进入登录页。
- 这个改动是去同质化的第一步：先去掉最容易暴露 TechSpar 来源的开场体验，再逐步重写 QTrace 自己的工作台外壳和核心页面；不能把“换 Logo”当成真正的独立实现。

本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或 GitHub commit/push。

## 阶段 85：QTrace 自有工作台外壳

- 我先复查阶段 84 的根路径契约，确认启动动画仍未被 `App.tsx` 引用，未登录 `/` 直接进入 `/login`；因此本阶段没有重新触碰入口逻辑。
- 我新增 `QTraceWorkspaceShell` 和 `qtrace-workspace.css`，把活动 AppShell 从 TechSpar 的 `Sidebar` 切换到 QTrace 自己的工作台：导航按训练路径、长期记忆、素材与图谱分组，顶部显示当前工作路径，账户/主题/设置操作收敛在壳层；移动端使用抽屉，不依赖旧侧栏实现。
- 我保留页面、认证、ProviderGate、路由和 QTrace API adapter，说明“先替换壳层，再逐页迁移”是一个可回滚的边界：AppShell 的视觉与导航可以独立验证，不把训练状态机和接口同时改坏。
- 我把旧的 TechSpar 文件集合预检改成参考审计，新增 `qtrace_shell_preflight.py` 作为阻断性契约。这样参考项目只提供交互证据，活动外壳的所有权由 QTrace 源码和测试明确表达。
- 验证证据：shell/旧预检回归 `5 passed`，两个静态预检通过，前端 typecheck 通过；标准 build 在既有 `node_modules/.vite-temp`/正式 `dist` 写入点遇到 Windows `EPERM`，未删除依赖或构建目录，改用 `--configLoader runner` 和暂存输出目录成功转换 3808 个模块，仅有大型 bundle 提示。Windows 系统 `.pytest_cache` 的 WinError 5 仍是权限警告，使用项目暂存 basetemp 可得到稳定结果。
- 我现在能回答：为什么不能用“与参考仓库文件集合完全相同”证明前端正确、为什么 AppShell 是适合先切换的深模块边界，以及为什么静态 shell 契约仍不能替代登录后的浏览器视觉验收。

本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或 GitHub commit/push。

## 阶段 86：QTrace 开始训练工作台

- 我把下一步选择为 `/mock-interview` 而不是直接重写体量很大的 `/profile`：它的业务边界清楚，只有两种模式选择和两个既有子流程，适合先验证“页面独立实现但 API 不动”的方法。
- 我保留 `live/targeted` URL 参数、左右/Home/End 键盘切换、`role=tablist/tab/tabpanel`、`ResumeInterview`、`JobPrep` 和专项训练跳转；改变的是页面编排和视觉层。
- 新页面用 `01 / INTERVIEW LOOP`、当前模式 readout、`02 / SELECT MODE` 和硬边模式条表达“先选目标，再推进一个主任务”；`qtrace-interview.css` 与自有 AppShell 共享纸张画布、碳黑文字和砖红信号色，避免继续依赖 TechSpar 页面里的圆角卡片节奏。
- 新增 `qtrace_interview_preflight.py` 与 2 条回归。shell/旧预检/训练页专项回归为 `7 passed`，两个静态预检、typecheck 和备用输出目录 build 通过；备用 build 转换 3809 个模块，只有大型 bundle 提示。正式 `dist` 的 Windows `EPERM` 写入限制没有通过删除依赖或构建产物解决。
- 我现在能回答：为什么先迁移页面编排层而不是把子流程一起重写、为什么 URL 参数和键盘交互属于业务契约、以及为什么“页面构建通过”仍不能代替登录后切换模式的浏览器验收。

本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或 GitHub commit/push。

## 阶段 87：QTrace 画像首页工作台

- 我把 `/profile` 的首屏作为下一块迁移边界：它体量大、包含很多画像派生，但用户首次看到的空状态和训练入口可以独立改造，不需要触碰画像 API 或 SM-2 计算。
- 我保留 `getProfile`、`getTopics`、`markProfileViewed`、画像派生函数、到期复习、知识证据、能力地图、行为模式和趋势组件；空状态的三个入口仍跳转 `/topic-drill`、`/resume-interview`、`/job-prep`。
- 新增 `00 / PERSONAL MEMORY` 标题层、QTrace 纸张面板、信号红状态 badge、硬边训练入口和统计左刻度；通过 `qtrace-profile.css` 去掉首屏渐变、厚重圆角和装饰性阴影，保持窄屏单列与 reduced-motion。
- 新增 `qtrace_profile_preflight.py` 与 2 条回归。画像/训练/shell 专项回归 `7 passed`，预检、typecheck、备用输出目录 build 和入口检查通过；备用 build 转换 3810 个模块，正式 dist 的既有 Windows `EPERM` 没有通过删除依赖解决。
- 我现在能回答：为什么画像页先迁移信息层而不是重写数据层、为什么空状态是产品闭环的一部分，以及为什么“保留派生逻辑”能降低前端去同质化对学习功能的风险。

本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或 GitHub commit/push。

本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或 GitHub commit/push。

## 阶段 88：QTrace 模型设置反馈工作台

- 用户此前点击本地语义 Embedding 后只能看到页面刷新，LLM 失败也缺少明确上下文。本阶段先检查已有 Settings 状态和 API adapter，确认测试、保存和 reindex 逻辑已经存在，问题主要在反馈可见性与设置页仍沿用旧卡片表现。
- `Settings.jsx` 新增 QTrace 首屏状态读数：LLM 连接、Embedding 模式/连接、配置保存分别显示待测试、测试中、已连接、连接失败或已保存；连接结果用 `role="status"`/`role="alert"` 表达，失败详情不再只埋在按钮旁边。
- 保存、加载、连接测试和向量重建错误统一经过 `redactSettingsError`，仅保留有限长度的诊断摘要并隐藏 Bearer/token 类凭据片段；没有增加任何 API Key 回显逻辑，也没有改变后端请求体或设置存储。
- `qtrace-settings.css` 将设置页改成 QTrace 自有的硬边纸张面板、信号红和三格状态读数，输入框/卡片/保存栏去圆角、去渐变、去阴影；窄屏状态区改为单列，并保留 reduced-motion。
- 新增 `qtrace_settings_preflight.py` 和 2 条回归；设置、入口、shell、开始训练、画像预检以及 final delivery preflight 通过，typecheck 通过，备用输出目录 build 成功转换 3811 个模块；5174 根路径 200、无 `/@vite/client`，`/api/health` 返回 qtrace healthy。
- 本阶段让我能回答：为什么“配置已保存”不等于“模型可用”、为什么 LLM/Embedding 测试要分开、为什么错误展示必须做脱敏，以及为什么设置页状态层是比换颜色更有价值的前端独立实现。

登录后的模型设置页视觉、本地 Embedding 成功/失败路径和真实 LLM 测试仍需用合成账号人工验收；本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或 GitHub commit/push。

## 阶段 89：本地验收清单与结束条件

- 我把“项目什么时候可以结束”从模糊的感觉改成 `docs/STAGE89_LOCAL_ACCEPTANCE_CHECKLIST.md`：启动入口、工作台、画像、开始训练、模型设置、Embedding reindex、文档导入、Personal Agent、知识图谱和主题/窄屏逐项验收。
- 自动证据和人工证据分开：静态预检、全量合成 Python 回归 `124 passed`、69 个 Python 文件内存编译、typecheck、备用构建、5174 登录入口和 `/api/health` 已经可以重复运行；登录后的合成账号路径、真实 LLM/本地 Embedding 成功与失败路径必须由用户在浏览器中勾选，不能用源码预检替代。
- 清单明确记录正式 `dist` 写入 `EPERM` 的当前 Windows 环境边界、真实资料/真实 API Key 禁止项，以及真实长音频、说话人分离和时间戳不属于当前交付范围。
- 本阶段没有新增业务代码，没有读取或输出 API Key、真实简历、个人文档或浏览器存储，没有调用外部 API、删除文件、部署或 GitHub commit/push。

## 阶段 90：面试 Demo 公网部署契约

- 用户目标从“本地能跑”推进到“面试官可以通过网页体验”。我先把部署问题拆成可验证的本地契约，不在没有云账号、域名和 HTTPS 配置时假装已经上线。
- 新增 Docker Compose 双服务：FastAPI 后端使用 `python:3.12-slim`，前端生产构建使用 Node 22 并由 Nginx 提供；Nginx 将同源 `/api` 请求代理到内部 `api:8000`，静态路由使用 `try_files` 回退到 `index.html`。
- 后端新增 `REBUILD_ALLOWED_ORIGINS` 配置；Compose 强制要求外部提供 `REBUILD_JWT_SECRET`，数据通过命名卷挂载到 `/app/data`，避免把 SQLite 文件打进镜像或暴露后端端口。
- 新增 `scripts/public_demo_preflight.py` 与回归，检查部署文件、反向代理、健康依赖、持久化卷、secret 占位符和示例配置中没有 LLM credential assignment；检查只读，不启动容器、不联网、不读取 SQLite/真实资料/API Key。
- 公开 Demo 的真实产品边界明确为 BYOK：用户可以在网页配置自己的 OpenAI-compatible API，后端调用真实 Provider；Stub 仅用于无 Key 的离线体验和自动化回归。正式公网前仍需补充 API Key 加密/会话保存、API Base 白名单与 SSRF 防护、限流、HTTPS、备份以及独立 LLM 连接测试接口。
- 本地验证补充完成：专项预检 `3 passed`；Docker Compose 配置解析通过；`docker compose build` 成功构建 API/Web 镜像；短暂启动后 8080 首页和同源 `/api/health` 代理均返回 200，随后仅停止容器且没有删除命名卷。正式工程 `frontend/dist` 因 Windows `EPERM` 未作为运行态资产生成，但容器内生产构建已经成功。
- 我现在能回答：为什么前端和后端要用同源反向代理、为什么生产 JWT secret 不能沿用本地默认值、为什么“Docker Compose 可启动”不等于“公网安全可用”，以及为什么 BYOK 仍需要服务端密钥治理。

本阶段只修改本地部署准备、配置、预检和文档；没有公网/外部部署、没有读取或输出 API Key/真实简历/个人文档、没有提交或推送 GitHub。

## 阶段 91：BYOK 独立 LLM 连接测试

- 复查模型设置链路后确认：真实 Provider 已用于训练，但 `testLLMConnection` 仍是前端占位。新增 `POST /api/settings/test-llm`，把当前表单的 API Base、Model 和 API Key 交给 `OpenAICompatibleProvider.probe()`，不写入 Store；空字段才回退到已保存配置，方便刷新页面后重新测试而不回显 Key。
- Provider 探测使用固定合成提示和 `max_tokens=1`，零重试，成功/失败返回都不包含 Authorization、API Key 或上游响应正文。前端 adapter 现在真实 fetch 该端点，已有 loading/success/error 状态可以观察真实结果。
- 新增 `scripts/qtrace_byok_preflight.py` 和回归；BYOK 静态契约 `2 passed`，Provider/端点合成回归 `8 passed`，全量合成回归 `133 passed`，前端 typecheck/build 通过。更新后的 Docker 容器也通过 8080 健康检查和空配置测试分支；没有调用真实 LLM API。
- 这一步只解决“能否独立验证连接”，没有假装解决公网密钥治理。公开 Demo 前仍需 API Key 加密或会话存储、API Base SSRF/私网阻断、限流、预算和受控的真实联调。

## 阶段 92：公开 Demo 的会话级 BYOK 存储

- 为避免访问者的 LLM/Embedding Key 明文落入公开 Demo 的 SQLite，新增 `REBUILD_BYOK_STORAGE_MODE`：`persisted` 保持本地兼容，`session` 只在 FastAPI 进程内存保存 Key；Compose 和示例环境默认 `session`。
- session 模式下非敏感的 API Base/Model 仍可用于恢复界面，Key 字段写入 SQLite 为空；服务重启后配置被判定为未完成，Provider 不会拿空 Key 发起请求，用户需要重新输入。
- 合成回归覆盖“内存可用、数据库为空、重启失效”和 persisted 兼容路径；公共 Demo 预检新增 session 配置与 Store 实现标记。全量回归 `135 passed`、typecheck/build 和容器健康检查继续作为门禁；本机容器实测重启 API 后配置回到未完成，说明不会从 SQLite 恢复 Key。
- 该改动降低了数据库泄露风险，但没有替代 HTTPS、API Base SSRF/私网阻断、限流、调用预算、审计日志和真实外部联调；这些仍是取得公网 URL 前的必要工作。

## 阶段 93：本地运行态交接与 session Key 状态清理

- 本地端口曾出现“5174 是当前前端、8002 仍是旧后端”的混合状态。通过 OpenAPI 和同源代理检查识别出旧实例后，只重启 QTrace 的 8002 监听进程，并将后端指向阶段暂存目录的合成 SQLite/Data 路径；没有触碰正式数据库或浏览器存储。
- 合成 HTTP 彩排通过：5174 `/api/health` 返回 qtrace healthy；新合成账号可以注册、读取设置；空配置 `POST /api/settings/test-llm` 返回安全的缺少配置错误；当前 OpenAPI 包含 LLM 连接测试和 PDF/Markdown 文档上传接口；前端入口没有 Vite 黑屏注入。
- 发现并修复 session BYOK 的边界：远程 Embedding 切换到 Demo 或本地模型后，必须清除进程内旧 Key，否则设置页的 `embedding_key_configured` 会残留为 true。新增回归覆盖 Demo 与本地模型两条切换路径，隔离全量回归 `136 passed`。
- 前端 typecheck/build 和 public demo、BYOK、final delivery 预检通过；正式目录的 Vite `.vite-temp`/默认 SQLite 写入仍受 Windows 权限边界影响，使用暂存输出/隔离环境验证。该限制已记录，不通过删除文件规避。
- 本阶段仍没有公网部署、域名、HTTPS、真实 LLM 联调、真实简历/个人文档、API Key 输出或 GitHub commit/push。下一步只剩用户用合成账号完成浏览器逐项验收，及未来有部署授权后补 SSRF、限流、预算和云端配置。

## 阶段 94：公开 Demo API Base SSRF 第一层防护

- 公开 Demo 不能把访问者输入的任意 API Base 直接交给 `httpx`，否则服务端可能被利用访问 localhost、云元数据、私网或内部 DNS。新增 `backend/network_policy.py`，统一校验协议、主机名、URL 凭据、IP 字面量和 DNS 解析结果。
- `REBUILD_BLOCK_PRIVATE_API_BASE` 默认关闭以保持本地 Ollama/兼容服务可调试；Compose 与 `deploy/demo.env.example` 默认开启。LLM 保存、Embedding 远程保存、LLM 独立连接测试和 Provider 构造共用该边界，私网地址在 Provider 构造前即失败。
- 安全专项回归 `23 passed`：覆盖回环/私网/链路本地/内部域名/不安全协议/URL 凭据、公开 DNS 结果、私网 DNS 结果和接口不触发 Provider；BYOK/public demo 静态预检同步检查网络策略标记。
- Docker 生产镜像构建和 Compose 短时运行通过：8080 首页、同源健康检查、合成注册均成功；公开模式下私有地址的 LLM 探测被拒绝、配置保存返回 400，验证后仅停止容器并保留数据卷。
- 最终收口门禁通过：全量合成回归 `148 passed`，BYOK/public-demo/final-delivery 预检和 `git diff --check` 通过；仅保留正式目录 `.pytest_cache` 的 Windows 写权限 warning，不删除权限受限产物。
- 这只是应用层第一道门。DNS 检查存在时间窗口，真正公网发布仍需云防火墙或 egress proxy 防止 DNS rebinding，并补 HTTPS、限流、调用预算、日志治理和监控；本阶段没有真实外部请求、真实资料、真实 API Key、部署或 GitHub 推送。

## 阶段 95：腾讯云 VPS 浏览器 Demo 部署准备

- 用户新增了独立 QTrace 腾讯云 VPS，目标从“可部署包”推进到“其他人可以用浏览器访问”。我把 Qidian 旧 VPS 明确排除在范围外，避免两个项目的容器、端口和安全组互相干扰。
- 新 VPS 的 Docker Hub 请求超时，但腾讯容器镜像、腾讯 PyPI 和 npmmirror 可达。部署 Dockerfile 现在通过 Compose build args 接收 `PIP_INDEX_URL`/`NPM_REGISTRY`；依赖镜像切换是部署配置，不改业务逻辑。
- 公网构建发现 `@fontsource/noto-serif-sc@5.3.0` 在 npmmirror 返回 404。移除该非业务必需依赖并改用系统衬线字体回退；Noto Sans 仍按原方式自托管。前端 typecheck/build、Compose config 通过。
- 云端验收必须继续区分：镜像构建成功 ≠ 容器健康 ≠ 浏览器可访问 ≠ 公网已上线。下一步只在新 VPS 上完成 Compose healthy、腾讯云安全组放行演示端口、获取真实公网 IPv4，并用合成账号从外部浏览器验收。
- 本阶段没有读取或输出 API Key、真实简历、个人文档或浏览器密码；远端环境 secret 不回显、不提交，公网 Demo 继续采用 session BYOK 和无 Key Stub 降级。

## 阶段 96：腾讯云公网浏览器验收

- 新 QTrace VPS 的 Compose 构建和启动成功：`qtrace-api-1` 为 healthy，`qtrace-web-1` 已启动；服务器内首页和 `/api/health` 均返回 200。
- 腾讯云防火墙现有 TCP 80 放行，因此没有额外修改安全组，而是在服务器私有 `deploy/demo.env` 中将 `QTRACE_DEMO_PORT` 设为 `80`。这避免了使用未放行的 8080，也没有触碰 Qidian 旧 VPS。
- 从外部 Chrome 访问 `http://49.232.104.202/` 成功，应用自动进入 `/login`，标题为“问迹 QTrace · 个性化面试训练”。因此当前项目已经达到“他人可以通过浏览器打开并看到登录入口”的面试 Demo 验收条件。
- VPS 到 GitHub 的 HTTPS 拉取曾在 45 秒门限内超时；为完成本次启动，远端工作树应用了与已推送 `main` 等价的最小构建修复。这个状态已记录，网络恢复后必须用 `git pull --ff-only` 重新建立可追踪同步，不能把手工远端修补当成 GitHub 提交。
- 本阶段没有读取或输出 API Key、JWT secret、真实简历、个人文档或浏览器密码；没有调用真实 LLM API，仅保留访问者自带 Key 的 BYOK 入口和无 Key 的 Stub 降级路径。
- 生产化仍未完成：当前是公网 IP + HTTP 80，不是域名/HTTPS/高可用服务；还需要补齐限流、预算、监控、备份和更严格的 BYOK egress/SSRF 治理。

## 阶段 97：首登模型配置暂时跳过

- 用户希望保留模型配置表单，但允许没有 API Key 的访客先进入系统。本阶段在 Onboarding 两个步骤底部新增“暂时跳过，稍后在设置中填写”，没有移除 API Base、Model、API Key 字段，也没有改变真实连接测试。
- 跳过状态按账号写入浏览器 localStorage，刷新页面可以继续浏览；退出登录时清除，避免一个账号的跳过状态影响另一个账号。后端仍会阻止未配置模型的训练/Agent 请求，前端可以引导用户前往模型设置。
- 本地 `frontend` typecheck 通过，前端测试 `3 passed`，Vite build 通过（3809 modules）；没有读取或输出 API Key、真实资料，没有真实 LLM 请求。
- 按用户要求，本阶段没有 commit/push GitHub，也没有更新或重启腾讯云 VPS；未来只有用户明确要求时才同步。
